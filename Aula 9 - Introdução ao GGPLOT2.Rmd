---
title: "Aula 8 - Introdução ao GGPLOT2"
author: "Claudiano Neto"
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  pdf_document: default
  html_document: default
subtitle: Adptado do '[A ggplot2 Tutorial for Beautiful Plotting in R ](https://www.cedricscherer.com/2019/08/05/a-ggplot2-tutorial-for-beautiful-plotting-in-r/)'
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE) 
```


## Introductory Words

[I don't care, just show me the content!](#toc)


## Table of Content {#toc}

* [Preparação](#prep)
* [O conjunto de dados](#data)
* [O Pacote `{ggplot2}`](#ggplot)
* [Um ggplot padrão](#default)
* [Trabalhando com Eixos](#axes)
* [Trabalhando com Titulos](#titles)
* [Trabalhando com Legendas](#legends)
* [Trabalhando com Planos de fundo e linhas de grade](#style)
* [Trabalhando com margens](#margins)
* [Trabalhando com Gráficos de vários painéis](#panels)
* [Trabalhando com Cores](#colors)
* [Trabalhando com Temas](#themes)
* [Trabalhando com Linhas](#lines)
* [Trabalhando com Texto](#text)
* [Trabalhando com Coordenadas](#coords)
* [Trabalhando com Tipos de Gráficos](#charts)
* [Trabalhando com Ribbons (AUC, CI, etc.)](#ribbons)
* [Trabalhando com Smoothings](#smooths)
* [Trabalhando com Plots Interativos](#interactive)
* [Observações, dicas e recursos](#tipps)



## Preparação {#prep}

* Você pode encontrar o script Rmarkdown com o código executado nesta postagem do blog [aqui] (https://github.com/Z3tt/Z3tt/blob/master/content/post/2019-08-05_ggplot2-tutorial.Rmd).
* Você também pode baixar o script R contendo apenas o código [aqui] (https://cedricscherer.netlify.app/codes/ggplot-tutorial-cedric-raw.R).
* Você precisa instalar os seguintes pacotes para executar o tutorial completo:
    + `{ggplot2}`, parte da coleção de pacotes `{tidyverse}`
    + `{tidyverse}` coleção de pacotes, a saber
      - `{dplyr}` para disputa de dados
      - `{tibble}` para quadros de dados modernos
      - `{tidyr}` para limpeza de dados
      - `{forcats}` para lidar com fatores
    + `{colorspace}` para manipulação de cores
    + `{corrr}` para calcular matrizes de correlação
    + `{cowplot}` para compor ggplots
    + `{ggdark}` para temas e cores invertidas
    + `{ggforce}` para gráficos sina e outras coisas legais
    + `{ggrepel}` para uma boa rotulagem de texto
    + `{ggridges}` para plotagens de cume
    + `{ggsci}` para belas paletas de cores
    + `{ggtext}` para renderização avançada de texto
    + `{ggthemes}` para temas adicionais
    + `{grid}` para a criação de objetos gráficos
    + `{gridExtra}` para funções adicionais para gráficos de "grade"
    + `{patchwork}` para gráficos de múltiplos painéis
    + `{rcartocolor}` para ótimas paletas de cores
    + `{scico}` para paletas uniformes de percepção
    + `{showtext}` para fontes personalizadas
    + `{brilhante}` para aplicativos interativos
    + uma série de pacotes para visualizações interativas
      + `{charter}`
      + `{echarts4r}`
      + `{ggiraph}`
      + `{highcharter}`
      + `{plotly}`

```{r install-packages, eval=FALSE}
# instalando do CRAN packages
# install.packages(c("tidyverse", "colorspace", "corrr",  "cowplot",
#                    "ggdark", "ggforce", "ggrepel", "ggridges", "ggsci",
#                    "ggtext", "ggthemes", "grid", "gridExtra", "patchwork",
#                    "rcartocolor", "scico", "showtext", "shiny",
#                    "plotly", "highcharter", "echarts4r"))
# # instalando do GitHub (aqueles fora do CRAN)
# install.packages(devtools)
# devtools::install_github("JohnCoene/charter")
```

(For teaching reasons and if people jump to any plot, I load the package needed beside `{ggplot2}` in the respective section.)



## O conjunto de Dados {#data}

We are using data from the *National Morbidity and Mortality Air Pollution Study* (NMMAPS). To make the plots manageable we are limiting the data to Chicago and 1997–2000. For more detail on this data set, consult Roger Peng's book [Statistical Methods in Environmental Epidemiology with R](http://www.springer.com/de/book/9780387781662).
You can download the data we are using during this tutorial [here](https://github.com/Z3tt/R-Tutorials/blob/master/ggplot2/chicago-nmmaps.csv) (but you don't have to).

We can import the data into our R session for example with `read_csv()` from the `{readr}` package. To access the data later, we are storing it in a variable called `chic` by using the *assignment arrow* `<-`.

Estamos usando dados do *National Morbidity and Mortality Air Pollution Study* (NMMAPS). Para tornar os gráficos gerenciáveis, estamos limitando os dados a Chicago e entre 1997–2000. Para obter mais detalhes sobre este conjunto de dados, consulte o livro de Roger Peng [Métodos Estatísticos em Epidemiologia Ambiental com R] (http://www.springer.com/de/book/9780387781662).
Você pode baixar os dados que estamos usando durante este tutorial [aqui](https://github.com/Z3tt/R-Tutorials/blob/master/ggplot2/chicago-nmmaps.csv), *mas você não precisa*.

Podemos importar os dados para nossa sessão R, por exemplo, com `read_csv()` do pacote `{readr}`. Para acessar os dados posteriormente, estamos armazenando-os em uma variável chamada `chic` usando a *seta de atribuição* `<-`.


```{r data-import, warning=FALSE, message=F}
chic <- readr::read_csv("https://raw.githubusercontent.com/Z3tt/R-Tutorials/master/ggplot2/chicago-nmmaps.csv")
```

💡 **O `::` é chamado de *namespace* e pode ser usado para acessar uma função sem carregar o pacote. Aqui, você também pode executar primeiro a função `library(readr)` e, depois, o comando `chic <- read_csv (...)`**

```{r data-inspection}
tibble::glimpse(chic)
head(chic, 10)
```


## O Pacote `{ggplot2}`{#ggplot}

> `ggplot2` é um sistema para criar gráficos declarativamente, baseado em [The Grammar of Graphics](https://link.springer.com/chapter/10.1007/978-3-642-21551-3_13). Você fornece os dados, diz ao `ggplot2` como mapear as variáveis para a estética, quais os elementos gráficos básicos usar o `ggplot2` cuida dos detalhes.
Um ggplot é construído a partir de alguns elementos básicos:

1.	**Data**:
Os dados brutos que você deseja plotar.
2.	**Geometries** `geom_`:
Os dados brutos que você deseja plotar.
3.	**Aesthetics** `aes()`:
Estética dos objetos geométricos e estatísticos, como posição, cor, tamanho, forma e transparência
4.	**Scales** `scale_`:
Mapas entre os dados e as dimensões estéticas, como intervalo de dados para plotar valores de largura ou fator para cores.
5.	**Statistical transformations** `stat_`:
Resumos estatísticos dos dados, como quantis, curvas ajustadas e somas.
6.	**Coordinate system** `coord_`:
A transformação usada para mapear coordenadas de dados no plano do retângulo de dados.
7.	**Facets** `facet_`:
A organização dos dados em uma grade de gráficos.
8.	**Visual themes** `theme()`:
Os padrões visuais gerais de um gráfico, como plano de fundo, grades, eixos, tipo de letra padrão, tamanhos e cores.

💡 **O número de elementos pode variar dependendo de como você os agrupa e a quem você pergunta.**

## Um ggplot padrão {#default}

Primeiro, para poder usar a funcionalidade de `{ggplot2}`, temos que carregar o pacote (que também podemos carregar por meio da [coleção de pacotes tidyverse](https://www.tidyverse.org/)):

```{r library, message=F, warning=F}
#library(ggplot2)
library(tidyverse)
```

A sintaxe de `{ggplot2}` é diferente do R base. De acordo com os elementos básicos, um ggplot padrão precisa de três coisas que você deve especificar: os *dados*, *estética* e uma *geometria*. Sempre começamos a definir um objeto de plotagem chamando `ggplot(data = df)` que apenas informa ao `{ggplot2}` que vamos trabalhar com esses dados. Na maioria dos casos, você pode querer representar graficamente duas variáveis - uma no eixo x e outra no eixo y. Essas são *estéticas posicionais* e, portanto, adicionamos `aes(x = var1, y = var2)` à chamada `ggplot()` (sim, `aes()` significa estética). No entanto, também existem casos em que é necessário especificar uma ou até três ou mais variáveis.

💡 **Especificamos os dados *fora* de `aes ()` e adicionamos as variáveis para as quais o ggplot mapeia a estética *dentro* de `aes ()`.**

Here, we map the variable `date` to the x position and the variable `temp` to the y position. Later, we will also map variables to all kind of other aesthetics such as color, size, and shape.

```{r ggplot}
(g <- ggplot(chic, aes(x = date, y = temp)))
```

Olha só, apenas um painel é criado ao executar isso. Por que? Isso ocorre porque `{ggplot2}` não sabe *como* queremos plotar esses dados — ainda precisamos fornecer uma geometria!

`ggplot2` permite que você armazene o `ggobject` atual em uma variável de sua escolha, atribuindo-o a uma variável, em nosso caso chamada `g`. Você pode estender este `ggobject` posteriormente adicionando outras camadas, todas de uma vez ou atribuindo-o à mesma ou a outra variável.

💡 **Usando parênteses ao atribuir um objeto, o objeto será impresso imediatamente (ao invés de escrever `g <- ggplot (...)` e então `g` nós simplesmente escrevemos` (g <- ggplot (...) ) `).**

Existem muitas, muitas geometrias diferentes (chamadas *geoms* porque cada função geralmente começa com `geom_`) que podem ser adicionadas a um ggplot por padrão (veja [aqui](https://ggplot2.tidyverse.org/reference/) para uma lista completa) e ainda mais fornecidos por pacotes de extensão (veja [aqui](https://exts.ggplot2.tidyverse.org/) para uma coleção de pacotes de extensão). Vamos dizer ao `{ggplot2}` qual estilo queremos usar, por exemplo, adicionando `geom_point()` para criar um gráfico de dispersão:

```{r ggplot-default}
g + geom_point()
```

Massa! Mas esses dados também podem ser visualizados como um gráfico linear (não é o ideal, mas as pessoas fazem coisas assim o tempo todo). Então, simplesmente adicionamos `geom_line()` e voilá:

```{r ggplot-default-line}
g + geom_line()
```

Nós também podemos combinar várias outras camadas geométricas - e é aqui que a magia e a diversão começam!

```{r ggplot-default-line-point}
g + geom_line() + geom_point()
```

Por enquanto, é isso sobre geometrias. Não se preocupe, vamos aprender vários tipos de plots em [um ponto posterior](#charts).


##### Alterar as propriedades das geometrias

Dentro do comando `geom_ *`, você já pode manipular a estética visual, como a cor, forma e tamanho de seus pontos. Vamos transformar todos os pontos em grandes diamantes vermelho-fogo!

```{r ggplot-default-col-size-shape}
g + geom_point(color = "firebrick", shape = "diamond", size = 2)
```

💡**`{ggplot2}` compreende `color` e` colour`, bem como a versão curta `col`. **

<details>
  <summary>&#128129; <i>You can use preset colors (here is a [full list](http://www.stat.columbia.edu/~tzheng/files/Rcolor.pdf)) or [hex color codes](https://www.techopedia.com/definition/29788/color-hex-code), both in quotes, and even RGB/RGBA colors by using the `rgb()` function. Expand to see example.</i></summary>
  
  <details>
   <summary>&#128129; <i>Você pode usar cores predefinidas (aqui está uma [lista completa] (http://www.stat.columbia.edu/~tzheng/files/Rcolor.pdf)) ou [códigos de cores hexadecimais] (https://www.techopedia.com/definition/29788/color-hex-code), ambos entre aspas e até mesmo cores RGB / RGBA usando a função `rgb ()`. Expanda para ver o exemplo.</i></summary>

```{r ggplot-default-col-size-hex-rgb, eval=FALSE}
g + geom_point(color = "#b22222", shape = "diamond", size = 2)
g + geom_point(color = rgb(178, 34, 34, maxColorValue = 255), shape = "diamond", size = 2)
```

```{r ggplot-default-col-size-hex-rgb-plot, echo=FALSE, purl=FALSE}
library(patchwork)
a <- g + geom_point(color = "#b22222", shape = "diamond", size = 2)
b <- g + geom_point(color = rgb(178, 34, 34, maxColorValue = 255), shape = "diamond", size = 2)
a + b
```

</details>

Cada geom vem com suas próprias propriedades (chamadas *argumentos*) e o mesmo argumento pode resultar em uma mudança diferente dependendo do geom que você está usando.

```{r ggplot-default-line_col-size-shape}
g + geom_point(color = "firebrick", shape = "diamond", size = 2) +
    geom_line(color = "firebrick", linetype = "dotted", size = .3)
```


##### Substitua o tema padrão `ggplot2`

E para ilustrar um pouco mais a versatilidade do ggplot, vamos nos livrar da aparência cinza padrão `{ggplot2}` definindo um tema integrado diferente, por exemplo, `theme_bw()` — chamando `theme_set()` todos os gráficos seguintes terão o mesmo tema preto e branco. Os pontos vermelhos parecem muito melhores agora!

```{r remove-gray-background}
theme_set(theme_bw())
g + geom_point(color = "firebrick")
```

Você pode saber mais sobre como usar temas embutidos e como personalizar temas na seção ["Trabalhando com Temas"](# temas). A partir do próximo capítulo, também usaremos a função `theme()` para personalizar elementos particulares do tema.

💡 **`theme()` é um comando essencial para modificar manualmente todos os tipos de elementos do tema (textos, retângulos e linhas).**

Para ver quais detalhes de um tema ggplot podem ser modificados, dê uma olhada [aqui] (https://ggplot2.tidyverse.org/reference/theme.html) — e demore algum tempo, esta é uma lista looonga.

[↑ Volte para o Índice.](#toc)<br><br>



## Trabalhando com Eixos {#axes}

##### Alterar os títulos dos eixos

Vamos adicionar alguns rótulos bem escritos aos eixos. Para isso, adicionamos `labs()` fornecendo uma string de caracteres para cada rótulo que queremos alterar (aqui, `x` e` y`):

```{r axis-labs}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)")
```

<details>
  <summary>&#128129; <i>Você também pode adicionar cada título de eixo via `xlab()` e `ylab()`. Expanda para ver um exemplo.</i></summary>
```{r axis-labs-2}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  xlab("Ano") +
  ylab("Temperatura (°F)")
```
</details>

Usually you can also specify symbols by simply adding the symbol itself (here "°") but the code below also allows to add not only symbols but e.g. superscripts:

```{r axis-labs-expression}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = expression(paste("Temperatura (", degree ~ F, ")"^"(Ei, por que devemos usar unidades métricas?!)")))
```


##### Aumente o espaço entre o eixo e os títulos do eixo

`theme()` é um comando essencial para modificar elementos específicos do tema (textos e títulos, caixas, símbolos, planos de fundo, ...). Vamos usá-los muito! Por enquanto, vamos modificar elementos de texto. Podemos alterar as propriedades de todos ou de elementos de texto específicos (aqui títulos de eixo) sobrescrevendo o `element_text()` padrão na chamada `theme()`:

```{r labs-move-away-vjust}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.title.x = element_text(vjust = 0, size = 15),
        axis.title.y = element_text(vjust = 2, size = 15))
```

`vjust` refere-se ao alinhamento vertical, que geralmente varia entre 0 e 1, mas você também pode especificar valores fora desse intervalo. Observe que, embora movamos o título do eixo no eixo y horizontalmente, precisamos especificar `vjust` (que está correto da perspectiva do rótulo). Você também pode alterar a distância especificando a margem de ambos os elementos de texto:

```{r labs-move-away-margin}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.title.x = element_text(margin = margin(t = 10), size = 15),
        axis.title.y = element_text(margin = margin(r = 10), size = 15))
```

Os rótulos `t` e` r` dentro do objeto `margin ()` referem-se a *topo* e *direita*, respectivamente. Você também pode especificar as quatro margens como `margem (t, r, b, l)`. Observe que agora temos que alterar a margem direita para modificar o espaço no eixo y, não a margem inferior.

💡 **Uma boa maneira de lembrar a ordem dos lados da margem é "*t*-*r*-oub-*l*-*e*".**


##### Alterar a estética dos títulos dos eixos

Novamente, usamos a função `theme()` e modificamos o elemento `axis.title` e / ou os elementos subordinados` axis.title.x` e `axis.title.y`. Dentro de `element_text()` podemos, por exemplo, sobrescrever os padrões para `size`,` color` e `face`:

```{r labs-color-axes-1}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.title = element_text(size = 15, color = "firebrick",
                                  face = "italic"))
```

O argumento `face` pode ser usado para tornar a fonte negritada - `bold`, ou itálico - `italic`, ou até mesmo negrito-itálico -  `bold.italic`.

```{r labs-color-axes-2}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.title.x = element_text(color = "sienna", size = 15),
        axis.title.y = element_text(color = "orangered", size = 15))
```

<details>
  <summary>&#128129; <i> You could also use a combination of `axis.title` and `axis.title.y`, since `axis.title.x` inherits the values from `axis.title`. Expand to see example.</i></summary>
```{r labs-color-axes-3}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.title = element_text(color = "sienna", size = 15),
        axis.title.y = element_text(color = "orangered", size = 15))
```
</details>

É possível modificar algumas propriedades para ambos os títulos dos eixos e outras apenas para um ou propriedades para cada um por si:

```{r labs-color-axes-4}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.title = element_text(color = "sienna", size = 15, face = "bold"),
        axis.title.y = element_text(face = "bold.italic"))
```


##### Alterar a estética do texto do eixo

Da mesma forma, você também pode alterar a aparência do texto do eixo (aqui *os números*) usando `axis.text` e / ou os elementos subordinados `axis.text.x` e `axis.text.y`:

```{r labs-color-axes-text}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.text = element_text(color = "dodgerblue", size = 12),
        axis.text.x = element_text(face = "italic"))
```


##### Girar Texto do Eixo

Especificar um `ângulo` permite girar qualquer elemento de texto. Com `hjust` e` vjust` você pode ajustar a posição do texto depois horizontalmente (0 = esquerda, 1 = direita) e verticalmente (0 = superior, 1 = inferior):

```{r axis-text}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.text.x = element_text(angle = 50, vjust = 1, hjust = 1, size = 12))
```


##### Remover Texto do Eixo e Tiquetaques

Pode haver raramente um motivo para fazer isso, mas é assim que funciona:

```{r axis-no-labs}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(axis.ticks.y = element_blank(),
        axis.text.y = element_blank())
```

Eu introduzi três elementos de tema - texto, linhas e retângulos - mas na verdade há mais um: `element_blank()` que remove o elemento (e, portanto, não é considerado um elemento oficial).

💡 **Se você quiser se livrar de um elemento de tema, o elemento é sempre `element_blank()`.**


##### Remover títulos de eixo

Poderíamos usar novamente `theme_blank()` mas é muito mais simples remover o rótulo na chamada `labs()` (ou `xlab()`):

```{r axis-no-title}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = NULL, y = "")
```

💡 **Observe que `NULL` remove o elemento (semelhante a `element_blank()`), enquanto as aspas vazias `""` manterão o espaçamento para o título do eixo e simplesmente não imprimirão nada.**

##### Limite do intervalo do eixo

Às vezes, você deseja <s>ampliar</s> para dar uma olhada mais de perto em alguns intervalos de seus dados. Você pode fazer isso sem subdividir seus dados:

```{r axis-limit}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  ylim(c(0, 50)) # limitando de 0 a 50
```

Alternativamente, você pode usar `scale_y_continuous(limits = c(0, 50))` ou `coord_cartesian(ylim = c(0, 50))`. O primeiro remove todos os pontos de dados fora do intervalo, enquanto o segundo ajusta a área visível e é semelhante a `ylim(c(0, 50))`. Você pode se perguntar: *Então, no final, ambos resultam no mesmo.* Mas não realmente, há uma diferença importante - compare os dois gráficos a seguir:

```{r axis-limit-comp, echo=FALSE, purl=FALSE}
p1 <- ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_y_continuous(limits = c(0, 50)) +
  ggtitle("scale_y_continuous(limits) or ylim()")
p2 <- ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  coord_cartesian(ylim = c(0, 50)) +
  ggtitle("coord_cartesian(ylim)")
library(patchwork)
p1 + p2
```

Você deve ter notado que à esquerda há algum buffer vazio em torno de seus limites y, enquanto à direita os pontos são plotados até a borda e até além. Isso ilustra perfeitamente o subconjunto (à esquerda) versus o zoom (à direita). Para mostrar por que isso é importante, vamos dar uma olhada em um tipo de gráfico diferente, um gráfico de caixa:

```{r axis-limit-comp-box, echo=FALSE, purl=FALSE}
p1 <- ggplot(chic, aes(x = factor(year), y = temp)) +
  geom_boxplot() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_y_continuous(limits = c(0, 50)) +
  ggtitle("scale_y_continuous(limits) or ylim()")
p2 <- ggplot(chic, aes(x = factor(year), y = temp)) +
  geom_boxplot() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  coord_cartesian(ylim = c(0, 50)) +
  ggtitle("coord_cartesian(ylim)")
p1 + p2
```

Hum. Porque `scale_x|y_continuous()` subconjuntos de dados primeiro, obtemos estimativas completamente diferentes (e erradas, pelo menos se no caso este não era seu objetivo) para os gráficos de caixa! Espero que você não precise voltar aos seus scripts antigos agora e verificar se *talvez* tenha manipulado seus dados enquanto traçava e relatou estatísticas de resumo incorretas em seu relatório, artigo ou tese ...

##### Forçar o gráfico para começar na origem

Relacionado a isso, você pode forçar R a plotar o gráfico começando na origem:

```{r origin}
library(tidyverse)
chic_high <- dplyr::filter(chic, temp > 25, o3 > 20)
ggplot(chic_high, aes(x = temp, y = o3)) +
  geom_point(color = "darkcyan") +
  labs(x = "Temperatura maior do que 25°F",
       y = "Ozônio superior a 20 ppb") +
  expand_limits(x = 0, y = 0)
```

<details>
  <summary>&#128129; <i>Usando `coord_cartesian(xlim = c(0, NA), ylim = c(0, NA))` levará ao mesmo resultado. Expanda para ver um exemplo.</i></summary>
  
```{r origin-coord}
library(tidyverse)
chic_high <- dplyr::filter(chic, temp > 25, o3 > 20)
ggplot(chic_high, aes(x = temp, y = o3)) +
  geom_point(color = "darkcyan") +
  labs(x = "Temperatura maior do que 25°F",
       y = "Ozônio superior a 20 ppb") +
  coord_cartesian(xlim = c(0, NA), ylim = c(0, NA))
```
</details>

Mas também podemos forçá-lo a começar *literalmente* na origem!

```{r origin-force}
ggplot(chic_high, aes(x = temp, y = o3)) +
  geom_point(color = "darkcyan") +
labs(x = "Temperatura maior do que 25°F",
       y = "Ozônio superior a 20 ppb") +
  expand_limits(x = 0, y = 0) +
  scale_x_continuous(expand = c(0, 0)) +
  scale_y_continuous(expand = c(0, 0)) +
  coord_cartesian(clip = "off")
```

💡 **O argumento `clip = "off"` em qualquer sistema de coordenadas, sempre começando com `coord_*`, permite desenhar fora da área do painel.**

Aqui, eu o chamo para ter certeza de que as marcas em `c(0, 0)` não são cortadas. Veja o [tópico no Twitter de Claus Wilke](https://twitter.com/clauswilke/status/991542952802619392?lang=en) para mais detalhes.


##### Eixos com a mesma escala

Para fins de demonstração, vamos representar graficamente a temperatura em relação à temperatura com algum ruído aleatório. O `coord_equal()` é um sistema de coordenadas com uma razão especificada que representa o número de unidades no eixo y equivalente a uma unidade no eixo x. O padrão, `proporção = 1`, garante que uma unidade no eixo x tenha o mesmo comprimento que uma unidade no eixo y:

```{r axes-equal}
ggplot(chic, aes(x = temp, y = temp + rnorm(nrow(chic), sd = 20))) +
  geom_point(color = "sienna") +
  labs(x = "Temperatura (°F)", y = "Temperatura (°F) + random noise") +
  xlim(c(0, 100)) + ylim(c(0, 150)) +
  coord_fixed()
```

Razões superiores a um tornam as unidades no eixo y mais longas do que as unidades no eixo x e vice-versa:

```{r axes-fixed-2, fig.height=3}
ggplot(chic, aes(x = temp, y = temp + rnorm(nrow(chic), sd = 20))) +
  geom_point(color = "sienna") +
  labs(x = "Temperatura (°F)", y = "Temperatura (°F) + random noise") +
  xlim(c(0, 100)) + ylim(c(0, 150)) +
  coord_fixed(ratio = 1/5)
```


##### Use uma função para alterar rótulos

Às vezes é útil alterar um pouco seus rótulos, talvez adicionando unidades ou sinais de porcentagem sem adicioná-los aos dados. Você pode usar uma função neste caso:

```{r labs-alt}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = NULL) +
  scale_y_continuous(label = function(x) {return(paste(x, "Graus Fahrenheit"))})
```

[↑ Volte para o Índice.](#toc)<br><br>



## Trabalhando com Titulos {#titles}

##### Adicionar um Título

Nós podemos adicionar um título via `ggtitle()` function:

```{r title}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  ggtitle("Temperaturas em Chicago")
```

Alternativamente, você pode usar `labs()`. Aqui você pode adicionar vários argumentos, por exemplo além disso, uma legenda, uma legenda e uma tag (bem como títulos de eixo, conforme mostrado antes):

```{r title-labs}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)",
       title = "Temperaturas em Chicago",
       subtitle = "Padrão sazonal de temperaturas diárias de 1997 a 2001",
       caption = "Dados: NMMAPS",
       tag = "Fig. 1")
```


##### Torne o título em negrito e adicione um espaço na linha de base

Novamente, uma vez que queremos modificar as propriedades de um elemento de tema, usamos a função `theme()` e como para os elementos de texto `axis.title` e` axis.text` modificamos a face da fonte e a margem. Todas as seguintes modificações de elementos do tema funcionam não apenas para o título, mas para todos os outros rótulos, como `plot.subtitle`,` plot.caption`, `plot.caption`,` legend.title`, `legend.text`, e `axis.title` e` axis.text`.

```{r title-bold}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)",
       title = "Temperaturas em Chicago") +
  theme(plot.title = element_text(face = "bold",
                                  margin = margin(10, 0, 10, 0),
                                  size = 14))
```

💡 **Uma boa maneira de lembrar a ordem dos argumentos de margem é " *t*-*r*-oub-*l*-*e*" que se assemelha à primeira letra dos quatro lados.**


##### Ajuste de Posição dos Títulos

O alinhamento geral (left, center, right) é controlado por `hjust` (que significa ajuste horizontal):

```{r title-adjust}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = NULL,
       title = "Temperaturas em Chicago",
       caption = "Dados: NMMAPS") +
  theme(plot.title = element_text(hjust = 1, size = 16, face = "bold.italic"))
```

Claro, lá também é possível ajustar o alinhamento vertical, controlado por `vjust`.

Desde 2019, o usuário pode especificar o alinhamento do título, subtítulo e legenda com base na área do painel (o padrão) ou na margem do gráfico por meio de `plot.title.position` e` plot.caption.position`. O último é na verdade a melhor escolha em termos de design na maioria dos casos e muitas pessoas ficaram muito felizes com esse novo recurso, pois especialmente com rótulos de eixo y muito longos, o alinhamento parece horrível:

```{r title-position-default}
(g <- ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  scale_y_continuous(label = function(x) {return(paste(x, "Degrees Fahrenheit"))}) +
  labs(x = "Ano", y = NULL,
       title = "Temperaturas em Chicago entre 1997 e 2001 em \n Graus Fahrenheit",
       caption = "Dados: NMMAPS") +
  theme(plot.title = element_text(size = 14, face = "bold.italic"),
        plot.caption = element_text(hjust = 0)))
```

```{r title-position-plot}
g + theme(plot.title.position = "plot",
          plot.caption.position = "plot")
```


##### Use uma fonte não tradicional em seu título

Você também pode usar fontes diferentes, não apenas a padrão fornecida pelo ggplot (e que difere entre os sistemas operacionais). Existem vários pacotes que o ajudam a usar fontes que estão instaladas em sua máquina (e você pode estar usando em seu programa de escritório). Aqui, eu uso o [pacote `showtext`](https://github.com/yixuan/showtext) que facilita o uso de vários tipos de fontes (TrueType, OpenType, Type 1, fontes da web, etc.) em R parcelas. Depois de carregar o pacote, você também precisa importar a fonte que deve ser instalada em seu dispositivo. Eu uso regularmente [fontes do Google](https://fonts.google.com/) que podem ser importadas com a função `font_add_google()`, mas você também pode adicionar outras fontes com `font_add()`. (Observe que mesmo no caso de usar fontes do Google, você deve instalar a fonte - e reiniciar o Rstudio - para usar a fonte.)

```{r showtext, message=F, warning=F}
library(showtext)
font_add_google("Playfair Display", ## name of Google font
                "Playfair")  ## name that will be used in R
font_add_google("Bangers", "Bangers")
```

Agora, podemos usar essas famílias de fontes usando - sim, você adivinhou certo - `theme()`:

```{r title-style, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)",
       title = "Temperaturas em Chicago",
       subtitle = "Temperaturas Diarias em Chicago entre 1997 e 2001 em graus Fahrenheit") +
  theme(plot.title = element_text(family = "Bangers", hjust = .5, size = 25),
        plot.subtitle = element_text(family = "Playfair", hjust = .5, size = 15))
```

Você também pode definir uma fonte não padrão para todos os elementos de texto de seus gráficos, para mais detalhes consulte a seção ["Trabalhando com Temas"](#themes). Vou usar *Roboto Condensed* como a nova fonte para todos os gráficos seguintes.

```{r default-font}
font_add_google("Roboto Condensed", "Roboto Condensed")
theme_set(theme_bw(base_size = 12, base_family = "Roboto Condensed"))
```

<p style='color:gray;'>(O pacote`{showtext}` é ótimo, mas com ele você precisa importar a fonte que deseja usar explicitamente com `{showtext}`. No entanto, parece [existem alguns detalhes técnicos que não são resolvidos de forma otimizada por `{showtext}`] (https://twitter.com/thomasp85/status/1355083725156077571) então você pode querer usar o pacote como último recurso.)</p>


##### Alterar espaçamento em texto multilinha

Você pode usar o argumento `lineheight` para alterar o espaçamento entre as linhas. Neste exemplo, as linhas foram espremidas juntas (altura da linha < 1).
```{r multiline-title, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  ggtitle("Temperaturas em Chicago\nde 1997 a 2001") +
  theme(plot.title = element_text(lineheight = .8, size = 16))
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Legendas {#legends}

Iremos codificar o enredo por cores com base na estação. Ou para expressá-lo de uma forma mais ggplot'ish: mapeamos a variável `season` para a `aes`thetic `color`. Uma coisa boa sobre `{ggplot2}` é que ele adiciona uma legenda por padrão ao mapear uma variável para uma estética. Você pode ver que, por padrão, o título da legenda é o que especificamos no argumento de cor:

```{r legend-default, message=F, warning=F}
ggplot(chic,
       aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)")
```


##### Desligar a legenda

Sempre uma das primeiras perguntas é: "Como posso me livrar da legenda?".

É muito fácil e sempre funciona com `theme (legend.position = "none")`:

```{r legend-none, message=F, warning=F}
ggplot(chic,
       aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.position = "none")
```

Você também pode usar `guias (color = "none")` ou `scale_color_discrete (guide = "none")` dependendo do caso específico. Embora a alteração do elemento do tema remova todas as legendas de uma vez, você pode remover legendas específicas com as últimas opções, mantendo algumas outras:

```{r legend-none-guides, message=F, warning=F}
ggplot(chic,
       aes(x = date, y = temp,
           color = season, shape = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  guides(color = "none")
```

Aqui por exemplo, mantivemos a legenda para as formas - `shape`, enquanto descarta a das cores - `color`.


##### Remover títulos de legenda

Como já aprendemos, use `element_blank()` para não desenhar *nada* no título:

```{r legend-title-off, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.title = element_blank())
```

<details>
  <summary>&#128129; <i>Você pode conseguir o mesmo definindo o nome da legenda para `NULL`, tanto via `scale_color_discrete (name = NULL)` ou `labs (color = NULL)`. Expanda para ver exemplos.</i></summary>
```{r legend-title-null, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_color_discrete(name = NULL)
```

```{r legend-title-labs-null, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  labs(color = NULL)
```
</details>

##### Mudar posição da legenda

Se você quiser colocar a legenda não à direita, pode-se usar `legend.position` como argumento em `theme`. As posições possíveis são "top", "right" (que é o padrão), "bottom" e "left".

```{r legend-top, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.position = "top")
```

Você também pode colocar a legenda dentro do painel especificando um vetor com as coordenadas `x` e` y` relativas variando de 0 (esquerda ou inferior) a 1 (direita ou superior):

```{r legend-inside, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)",
       color = NULL) +
  theme(legend.position = c(.15, .15),
        legend.background = element_rect(fill = "transparent"))
```

Aqui, também sobrescrevo o fundo da legenda branco padrão com um preenchimento *transparente* para garantir que a legenda não oculte nenhum ponto de dados.


##### Mudar a Direção da Legenda

Como você viu, a direção da legenda é por padrão vertical, mas horizontal quando você escolhe a posição "top" ou "bottom". Mas você também pode mudar a direção como quiser:

```{r legend-orientation, message=F, warning=F}

ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.position = c(.5, .97),
        legend.background = element_rect(fill = "transparent")) +
  guides(color = guide_legend(direction = "horizontal"))
```


##### Mudar o Estilo do Título da Legenda

Você pode alterar a aparência do título da legenda ajustando o elemento do tema `legend.title`:

```{r legend-style, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.title = element_text(family = "Playfair",
                                    color = "chocolate",
                                    size = 14, face = "bold"))
```


##### Alterar título da legenda

A maneira mais fácil de alterar o título da legenda é a camada `labs()`:

```{r legend-title-labs, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)",
       color = "Temporadas\nindicadas\npelas cores:") +
  theme(legend.title = element_text(family = "Playfair",
                                    color = "chocolate",
                                    size = 14, face = "bold"))
```

Os detalhes da legenda podem ser alterados via `scale_color_discrete(name = "title")` ou `guias(color = guide_legend ("title"))`:

```{r legend-title, eval=FALSE, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season))) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.title = element_text(family = "Playfair",
                                    color = "chocolate",
                                    size = 14, face = "bold")) +
  scale_color_discrete(name = "Temporadas\nindicadas\npelas cores:")
```


##### Alterar a ordem das chaves de legenda

Podemos conseguir isso alterando os níveis de `season`:

```{r legend-order, message=F, warning=F}
library(haven)
f <- factor(chic$season, levels = c("Winter", "Spring", "Summer", "Autumn"))

chic <- chic %>% 
  mutate(season2 = forcats::fct_relevel(f)) 

ggplot(chic) + aes(x = date, y = temp, colour = season) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)")
```


##### Alterar Rótulos da Legenda

Vamos substituir as estações pelos meses que cobrem, fornecendo um vetor de nomes na chamada `scale_color_discrete()`:

```{r legend-labels, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_color_discrete(
    name = "Estações:", 
    labels = c("Mar—Mai", "Jun—Ago", "Set—Nov", "Dez—Fev")
  ) +
  theme(legend.title = element_text(
    family = "Playfair", color = "chocolate", size = 14, face = 2
  ))
```


##### Alterar caixas de fundo na legenda

Para alterar a cor de fundo (preenchimento) das chaves de legenda, ajustamos a configuração do elemento de tema `legend.key`:

```{r legend-boxes, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.key = element_rect(fill = "darkgoldenrod1"),
        legend.title = element_text(family = "Playfair",
                                    color = "chocolate",
                                    size = 14, face = 2)) +
  scale_color_discrete("Estações:")
```

Se você quiser se livrar deles completamente, use `fill = NA` ou `fill = "transparent"`.


##### Alterar o tamanho dos símbolos da legenda

Os pontos na legenda podem se perder um pouco com o tamanho padrão, especialmente sem as caixas. Para substituir o padrão, usa-se novamente a camada `guides` como esta:

```{r legend-symbols, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.key = element_rect(fill = NA),
        legend.title = element_text(color = "chocolate",
                                    size = 14, face = 2)) +
  scale_color_discrete("Estações:") +
  guides(color = guide_legend(override.aes = list(size = 6))) ##
```


##### Deixe uma camada fora da legenda

Digamos que você tenha dois geoms diferentes mapeados para a mesma variável. Por exemplo, a cor como uma estética para uma camada de pontos e uma camada de tapete dos mesmos dados. Por padrão, os pontos e a "linha" terminam na legenda assim:

```{r legend-layer-1, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  geom_rug()
```

Você pode usar `show.legend = FALSE` para desligar uma camada na legenda:

```{r legend-layer-2, message=F, warning=F}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  geom_rug(show.legend = FALSE)
```


##### Adicionar itens de legenda manualmente

`{ggplot2}` não adicionará uma legenda automaticamente a menos que você mapeie a estética (color, size, etc.) para uma variável. Há momentos, porém, em que quero ter uma legenda para que fique claro o que você está tramando.

Aqui está o padrão:

```{r legend-default-2}
ggplot(chic, aes(x = date, y = o3)) +
  geom_line(color = "gray") +
  geom_point(color = "darkorange2") +
  labs(x = "Ano", y = "Ozônio")
```

Podemos forçar uma legenda mapeando um guia para uma *variável*. Estamos mapeando as linhas e os pontos usando `aes()` e estamos mapeando **não** para uma variável em nosso conjunto de dados, mas para uma única string (de modo que obtemos apenas uma cor para cada).

```{r legend-force}
ggplot(chic, aes(x = date, y = o3)) +
  geom_line(aes(color = "line")) +
  geom_point(aes(color = "points")) +
  labs(x = "Ano", y = "Ozônio") +
  scale_color_discrete("Tipo:")
```

Estamos chegando perto, mas não é isso que queremos. Queremos cinza e vermelho! Para mudar a cor, usamos `scale_color_manual()`. Além disso, substituímos a estética da legenda usando a função `guide()`.

**Pronto!** Agora, temos um gráfico com linhas cinzas e pintas vermelhas, bem como uma única linha cinza e um único ponto vermelho como símbolos de legenda:

```{r legend-manual}
ggplot(chic, aes(x = date, y = o3)) +
  geom_line(aes(color = "line")) +
  geom_point(aes(color = "points")) +
  labs(x = "Ano", y = "Ozônio") +
  scale_color_manual(name = NULL,
                     guide = "legend",
                     values = c("points" = "darkorange2",
                                "line" = "gray")) +
  guides(color = guide_legend(override.aes = list(linetype = c(1, 0),
                                                  shape = c(NA, 16))))
```


##### Use outros estilos de legenda

A legenda padrão para variáveis categóricas como `season` é` guide_legend() `como você viu em vários exemplos anteriores. Se você mapear uma variável contínua para uma estética, `{ggplot2}` não usará, por padrão, `guide_legend()`, mas `guide_colorbar()` (ou `guide_colourbar()`):

```{r legend-guide-cont-default}
ggplot(chic,
       aes(x = date, y = temp, color = temp)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)", color = "Temperatura (°F)")
```

No entanto, usando `guide_legend()` você pode forçar a legenda a mostrar cores discretas para um determinado número de quebras, como no caso de uma variável categórica:

```{r legend-guide-cont-legend}
ggplot(chic,
       aes(x = date, y = temp, color = temp)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)", color = "Temperatura (°F)") +
  guides(color = guide_legend())
```

Você também pode utilizar *binned scales*:

```{r legend-guide-cont-bins}
ggplot(chic,
       aes(x = date, y = temp, color = temp)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)", color = "Temperatura (°F)") +
  guides(color = guide_bins())
```

... or binned scales as *discrete colorbars*:

```{r legend-guide-cont-steps}
ggplot(chic,
       aes(x = date, y = temp, color = temp)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)", color = "Temperatura (°F)") +
  guides(color = guide_colorsteps())
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Backgrounds & Linhas de Grade {#style}

Existem maneiras de mudar toda a aparência do seu enredo com uma função (consulte a seção ["Trabalhando com temas"](#temas) abaixo), mas se você quiser simplesmente mudar as cores de alguns elementos, também pode fazer isso.


##### Alterar a cor de fundo do painel

Para alterar a cor de fundo (`fill`) da área do painel (ou seja, a área onde os dados são plotados), é necessário ajustar o elemento de tema `panel.background`:

```{r panel-color}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "#1D8565", size = 2) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.background = element_rect(
    fill = "#64D2AA", color = "#64D2AA", size = 2)
  )
```

Observe que a cor verdadeira - o contorno do plano de fundo do painel - não mudou, embora tenhamos especificado. Isso ocorre porque há uma camada no topo do `panel.background`, chamada` panel.border`. No entanto, certifique-se de usar um preenchimento transparente aqui, caso contrário, seus dados ficarão ocultos atrás desta camada. No exemplo a seguir, ilustro isso usando uma cor hexadecimal semitransparente para o argumento `fill` em` element_rect`:

```{r panel-color-2}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "#1D8565", size = 2) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.border = element_rect(
    fill = "#64D2AA99", color = "#64D2AA", size = 2)
  )
```


##### Alterar Linhas de Grade

Existem dois tipos de linhas de grade: linhas de grade principais indicando as marcas e linhas de grade secundárias entre as principais. Você pode alterar tudo isso substituindo os padrões para `panel.grid` ou para cada conjunto de linhas de grade separadamente,` panel.grid.major` e `panel.grid.minor`.

```{r grid-lines}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.grid.major = element_line(color = "gray10", size = .5),
        panel.grid.minor = element_line(color = "gray70", size = .25))
```

Você pode até especificar configurações para todos os quatro níveis diferentes:

```{r grid-lines-x-y}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.grid.major = element_line(size = .5, linetype = "dashed"),
        panel.grid.minor = element_line(size = .25, linetype = "dotted"),
        panel.grid.major.x = element_line(color = "red1"),
        panel.grid.major.y = element_line(color = "blue1"),
        panel.grid.minor.x = element_line(color = "red4"),
        panel.grid.minor.y = element_line(color = "blue4"))
```

E, claro, você pode remover algumas ou todas as linhas de grade se desejar:

```{r grid-remove}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.grid.minor = element_blank())
```

```{r grid-blank}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.grid = element_blank())
```


##### Alterar espaçamento das linhas de grade

Além disso, você também pode definir as quebras entre as linhas de grade principais e secundárias:

```{r grid-breaks}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_y_continuous(breaks = seq(0, 100, 10),
                     minor_breaks = seq(0, 100, 2.5))
```


##### Alterar a cor de fundo do gráfico

Da mesma forma, para alterar a cor de fundo (preenchimento) da área de plotagem, é necessário modificar o elemento do tema `plot.background`:

```{r background-color}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(plot.background = element_rect(fill = "gray60",
                                       color = "gray30", size = 2))
```

Você pode obter uma cor de fundo exclusiva configurando as mesmas cores em `panel.background` e` plot.background` ou configurando o preenchimento do fundo do painel para `"transparente"` ou `NA`:

```{r background-color-same}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(panel.background = element_rect(fill = NA),
        plot.background = element_rect(fill = "gray60",
                                       color = "gray30", size = 2))
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Margens {#margins}

Às vezes, é útil adicionar um pouco de espaço à margem do gráfico. Semelhante aos exemplos anteriores, podemos usar um argumento para a função `theme()`. Neste caso, o argumento é `plot.margin`. Como no exemplo anterior, já ilustramos a margem padrão alterando a cor de fundo usando `plot.background`.

Agora vamos adicionar espaço extra à esquerda e à direita. O argumento, `plot.margin`, pode lidar com uma variedade de unidades diferentes (cm, polegadas, etc.), mas requer o uso da unidade de função do pacote` grade` para especificar as unidades. Você pode fornecer o mesmo valor para todos os lados (mais fácil via `rep(x, 4)`) ou distâncias específicas para cada um. Aqui estou usando uma margem de 1 cm na parte superior e inferior, margem de 3 cm na direita e uma margem de 8 cm na esquerda.

```{r margin}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(plot.background = element_rect(fill = "gray60"),
        plot.margin = margin(t = 1, r = 3, b = 1, l = 8, unit = "cm"))
```

A ordem dos lados da margem é top, right, bottom, right - uma boa maneira de lembrar essa ordem é "**tr**ou**bl**e que classifica a primeira letra dos quatro lados.

<details>
  <summary>&#128129; <i>Você também pode usar `unit()` ao invés de  `margin()`. Expanda para ver o exemplo</i></summary>
```{r margin-2}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(plot.background = element_rect(fill = "gray60"),
        plot.margin = unit(c(1, 3, 1, 8), "cm"))
```
</details>



[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Gráficos de vários painéis {#panels}

O pacote `{ggplot2}` tem duas funções interessantes para a criação de gráficos de múltiplos painéis, chamadas *facets*. Eles estão relacionados, mas um pouco diferentes: `facet_wrap` cria essencialmente uma faixa de gráficos com base em uma única variável, enquanto` facet_grid` abrange uma grade de duas variáveis.


##### Crie uma grade de pequenos múltiplos com base em duas variáveis

No caso de duas variáveis, `facet_grid` faz o trabalho. Aqui, a ordem das variáveis determina o número de linhas e colunas:

```{r grid-plots}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "orangered", alpha = .3) +
  theme(axis.text.x = element_text(angle = 45, vjust = 1, hjust = 1)) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  facet_grid(year ~ season)
```

Para alterar o arranjo de linha para coluna, você pode alterar `facet_grid(year ~ season)` para `facet_grid(season ~ year)`.


##### Criar pequenos múltiplos com base em uma variável

`facet_wrap` cria uma faceta de uma única variável, escrita com um til na frente:`facet_wrap(~ variável)`. A aparência desses subplots é controlada pelos argumentos `ncol` e `nrow`:

```{r wrap-plots-1-row}
g <- 
  ggplot(chic, aes(x = date, y = temp)) +
    geom_point(color = "chartreuse4", alpha = .3) +
    labs(x = "Ano", y = "Temperatura (°F)") +
    theme(axis.text.x = element_text(angle = 45, vjust = 1, hjust = 1))
g + facet_wrap(~ year)
```
Assim, você pode organizar os gráficos como quiser, em vez de uma matriz em uma linha ...

```{r wrap-plots-2-rows}
g + facet_wrap(~ year, nrow = 1)
```

... ou mesmo como uma grade assimétrica de parcelas:

```{r wrap-plots-2-rows-3-col}
g + facet_wrap(~ year, ncol = 3) + theme(axis.title.x = element_text(hjust = .15))
```


##### Permitir que os eixos circulem livremente

O padrão para gráficos de múltiplos painéis em `{ggplot2}` é usar escalas equivalentes em cada painel. Mas às vezes você deseja permitir que os próprios dados do painel determinem a escala. Freqüentemente, isso não é uma boa ideia, pois pode dar ao usuário uma impressão errada sobre os dados. Mas às vezes é realmente útil e para fazer isso você pode definir `escalas = "free"`:

```{r wrap-plots-scales-free}
g + facet_wrap(~ year, nrow = 2, scales = "free")
```

Observe que os eixos x e y diferem em seu intervalo!


#### Use `facet_wrap` com duas variáveis

A função `facet_wrap` também pode levar duas variáveis:

```{r wrap-plots-two-vars, fig.height=7}
g + facet_wrap(year ~ season, nrow = 4, scales = "free_x")
```

Ao usar `facet_wrap`, você ainda é capaz de controlar o design da grade: você pode reorganizar o número de plotagens por linha e coluna e também pode deixar todos os eixos vagarem livremente. Em contraste, `facet_grid` também receberá um argumento `free`, mas apenas o deixará livre por coluna ou linha:

```{r grid-plots-two-vars, fig.height=7}
g + facet_grid(year ~ season, scales = "free_x")
```



##### Modificar estilo de textos de tira

Usando `theme`, você pode modificar a aparência do texto da tira (ou seja, o título de cada faceta) e das caixas de texto da tira:

```{r facet-modify-striptext, fig.height=3.5}
g + facet_wrap(~ year, nrow = 1, scales = "free_x") +
  theme(strip.text = element_text(face = "bold", color = "chartreuse4",
                                  hjust = 0, size = 20),
        strip.background = element_rect(fill = "chartreuse3", linetype = "dotted"))
```

As seguintes [duas funções adaptadas desta resposta de Claus Wilke](https://stackoverflow.com/questions/60332202/conditionally-fill-ggtext-text-boxes-in-facet-wrap), o autor do [`{ ggtext} `package](https://wilkelab.org/ggtext/), permite destacar rótulos específicos em combinação com` element_textbox() `que é fornecido por` {ggtext} `.

```{r facet-color-striptext-function}
library(ggtext)
library(rlang)
element_textbox_highlight <- function(..., hi.labels = NULL, hi.fill = NULL,
                                      hi.col = NULL, hi.box.col = NULL, hi.family = NULL) {
  structure(
    c(element_textbox(...),
      list(hi.labels = hi.labels, hi.fill = hi.fill, hi.col = hi.col, hi.box.col = hi.box.col, hi.family = hi.family)
    ),
    class = c("element_textbox_highlight", "element_textbox", "element_text", "element")
  )
}
element_grob.element_textbox_highlight <- function(element, label = "", ...) {
  if (label %in% element$hi.labels) {
    element$fill <- element$hi.fill %||% element$fill
    element$colour <- element$hi.col %||% element$colour
    element$box.colour <- element$hi.box.col %||% element$box.colour
    element$family <- element$hi.family %||% element$family
  }
  NextMethod()
}
```

Agora você pode usá-lo e especificar, por exemplo, todos os textos de faixa:

```{r facet-color-striptext-A, fig.height=8}
g + facet_wrap(year ~ season, nrow = 4, scales = "free_x") +
  theme(
    strip.background = element_blank(),
    strip.text = element_textbox_highlight(
      family = "Playfair", size = 12, face = "bold",
      fill = "white", box.color = "chartreuse4", color = "chartreuse4",
      halign = .5, linetype = 1, r = unit(5, "pt"), width = unit(1, "npc"),
      padding = margin(5, 0, 3, 0), margin = margin(0, 1, 3, 1),
      hi.labels = c("1997", "1998", "1999", "2000"),
      hi.fill = "chartreuse4", hi.box.col = "black", hi.col = "white"
    )
  )
```

```{r r-facet-color-striptext-B, fig.height=3.5}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(aes(color = season == "Summer"), alpha = .3) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  facet_wrap(~ season, nrow = 1) +
  scale_color_manual(values = c("gray40", "firebrick"), guide = "none") +
  theme(
    axis.text.x = element_text(angle = 45, vjust = 1, hjust = 1),
    strip.background = element_blank(),
    strip.text = element_textbox_highlight(
      size = 12, face = "bold",
      fill = "white", box.color = "white", color = "gray40",
      halign = .5, linetype = 1, r = unit(0, "pt"), width = unit(1, "npc"),
      padding = margin(2, 0, 1, 0), margin = margin(0, 1, 3, 1),
      hi.labels = "Summer", hi.family = "Bangers",
      hi.fill = "firebrick", hi.box.col = "firebrick", hi.col = "white"
    )
  )
```


##### Crie um painel de diferentes plotagens

Existem várias maneiras de combinar os gráficos. A abordagem mais fácil, em minha opinião, é o [`{patchwork}` package](https://github.com/thomasp85/patchwork) de Thomas Lin Pedersen:

```{r combine-plots-patchwork}
p1 <- ggplot(chic, aes(x = date, y = temp,
                       color = season)) +
        geom_point() +
        geom_rug() +
        labs(x = "Ano", y = "Temperatura (°F)")
p2 <- ggplot(chic, aes(x = date, y = o3)) +
        geom_line(color = "gray") +
        geom_point(color = "darkorange2") +
        labs(x = "Ano", y = "Ozônio")
library(patchwork)
p1 + p2
```

Podemos alterar a ordem "dividindo" os dois gráficos (e observe o alinhamento, mesmo que um tenha uma legenda e o outro não!):

```{r combine-plots-patchwork-2, fig.height=8}
p1 / p2
```

E também gráficos aninhados são possíveis!

```{r combine-plots-patchwork-3, fig.height=6}
(g + p2) / p1
```

(Observe o alinhamento dos gráficos, embora apenas um gráfico inclua uma legenda.)

Alternativamente, o pacote [`{cowplot}`](https://cran.r-project.org/web/packages/cowplot/vignettes/introduction.html) de Claus Wilke fornece a funcionalidade para combinar vários gráficos (e muitos outros bons utilitários):

```{r combine-plots-cowplot}
library(cowplot)
plot_grid(plot_grid(g, p1), p2, ncol = 1)
```

... e o mesmo acontece com [`{gridExtra}` package](https://cran.r-project.org/web/packages/gridExtra/vignettes/arrangeGrob.html):

```{r combine-plots-grid}
library(gridExtra)
grid.arrange(g, p1, p2,
             layout_matrix = rbind(c(1, 2), c(3, 3)))
```

A mesma ideia de definir um layout pode ser usada com `{patchwork}` que permite a criação de composições complexas:

```{r combine-plots-patchwork-layout, fig.width=12, fig.height=8}
layout <- "
AABBBB#
AACCDDE
##CCDD#
##CC###
"
p2 + p1 + p1 + g + p2 +
  plot_layout(design = layout)
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Colors {#colors}

Para aplicações simples, Trabalhando com cores é direto no `{ggplot2}`. Para um tratamento mais avançado do tópico, você provavelmente deve colocar as mãos no [livro de Hadley](http://www.springer.com/de/book/9780387981413#otherversion=9780387981406), que tem uma boa cobertura. Outras boas fontes são o [R Cookbook](http://www.cookbook-r.com/Graphs/Colors_ (ggplot2)/) e a [`seção de cores na R Graph Gallery](https://www.r-graph-gallery.com/ggplot2-color.html) por Yan Holtz.

Existem duas diferenças principais quando se trata de cores em `{ggplot2}`. Ambos os argumentos, `color` e` fill`, podem ser

1. especificado como cor única ou
2. atribuído a variáveis.

Como você já viu no início deste tutorial, as variáveis que estão *dentro* da `aes`thetics são codificadas por variáveis e aquelas que estão *fora* são propriedades não relacionadas às variáveis. Este gráfico sem sentido completo mostrando o número de registros por ano e estação ilustra esse fato:

```{r inside-outside-aes}
ggplot(chic, aes(year)) +
  geom_bar(aes(fill = season), color = "grey", size = 2) +
  labs(x = "Ano", y = "Observações", fill = "Temporada:")
```


##### Especificar cores únicas

Cores únicas estáticas são fáceis de usar. Podemos especificar uma única cor para um geom:

```{r color-static}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "steelblue", size = 2) +
  labs(x = "Ano", y = "Temperatura (°F)")
```

... e caso forneça ambos, uma `color` (cor de contorno) e um` fill` (cor de preenchimento):

```{r color-fill-static}
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(shape = 21, size = 2, stroke = 1,
             color = "#3cc08f", fill = "#c08f3c") +
  labs(x = "Ano", y = "Temperatura (°F)")
```

Tian Zheng, da Columbia, criou um útil [PDF de cores R](http://www.stat.columbia.edu/~tzheng/files/Rcolor.pdf). Claro, você também pode especificar códigos de cores hexadecimais (simplesmente como strings como no exemplo acima), bem como valores RGB ou RGBA (por meio da função `rgb()`: `rgb(red, green, blue, alpha)`) .


##### Atribuir cores às variáveis

Em `{ggplot2}`, as cores que são atribuídas às variáveis são modificadas por meio das funções `scale_color_ *` e `scale_fill_ *`. Para usar cores com seus dados, o mais importante é saber se você está lidando com uma variável categórica ou contínua. A paleta de cores deve ser escolhida dependendo do tipo da variável, com paletas de cores sequenciais ou divergentes sendo usadas para variáveis contínuas e paletas de cores qualitativas para variáveis categóricas:


##### Variáveis Qualitativas

Variáveis qualitativas ou categóricas representam tipos de dados que podem ser divididos em grupos (*categorias*). A variável pode ser posteriormente especificada como nominal, ordinal e binária (dicotômica). Exemplos de variáveis qualitativas / categóricas são:

A paleta de cores categóricas padrão tem a seguinte aparência:

```{r color-cat-default}
(ga <- ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)", color = NULL))
```


###### Selecione manualmente as cores qualitativas

Você pode escolher seu próprio conjunto de cores e atribuí-las a variáveis categóricas por meio da função `escale_*_manual()` (o `*` pode ser `color`,` colour` ou `fill`). O número de cores especificadas deve corresponder ao número de categorias:

```{r color-cat-manual}
ga + scale_color_manual(values = c("dodgerblue4",
                                   "darkolivegreen4",
                                   "darkorchid3",
                                   "goldenrod1"))
```


###### Use paletas de cores qualitativas integradas

As [paletas ColorBrewer](http://colorbrewer2.org/) é uma ferramenta online popular para selecionar esquemas de cores para mapas. Os diferentes conjuntos de cores foram projetados para produzir esquemas de cores atraentes de aparência semelhante, variando de três a doze. Essas paletas estão disponíveis como funções integradas no pacote `{ggplot2}` e podem ser aplicadas chamando `scale_*_brewer()`:

```{r color-brewer}
ga + scale_color_brewer(palette = "Set1")
```

💡 **Você pode explorar todos os esquemas disponíveis via `RColorBrewer::display.brewer.all()`.**

###### Use paletas de cores qualitativas de pacotes de extensão

Existem muitos pacotes de extensão que fornecem paletas de cores adicionais. Seu uso difere dependendo da forma como a embalagem é projetada. Para uma ampla visão geral das paletas de cores disponíveis em R, verifique a [coleção fornecida por Emil Hvitfeldt](https://github.com/EmilHvitfeldt/r-color-palettes/blob/master/README.md#comprehensive-list-of -color-palettes-in-r). Também se pode usar seu pacote [`{paletteer}`](https://github.com/EmilHvitfeldt/paletteer), uma coleção abrangente de paletas de cores em R que usa uma sintaxe consistente.

**Exemplos:**

O pacote [`{ggthemes}`](https://jrnold.github.io/ggthemes/), por exemplo, permite que os usuários R acessem as cores do Tableau. Tableau é um software de visualização famoso com uma [paleta de cores conhecida](http://www.tableau.com/de-de/about/blog/2016/7/colors-upgrade-tableau-10-56782).
```{r color-tableau}
library(ggthemes)
ga + scale_color_tableau()
```

O [`{ggsci}` pacote](https://cran.r-project.org/web/packages/ggsci/vignettes/ggsci.html) fornece jornais científicos e paletas de cores com temas de ficção científica. Quer ter um enredo com cores que parecem estar sendo publicado na *Science* ou *Nature*? Aqui está!

```{r color-science-nature, fig.width=10}
library(ggsci)
g1 <- ga + scale_color_aaas()
g2 <- ga + scale_color_npg()
library(patchwork)
(g1 + g2) * theme(legend.position = "top")
```


##### Variáveis Quantitativas

As variáveis quantitativas representam uma quantidade mensurável e, portanto, são numéricas. Os dados quantitativos podem ser classificados como contínuos (números flutuantes possíveis) ou discretos (apenas números inteiros):

Em nosso exemplo, vamos mudar a variável que queremos colorir para Ozônio, uma variável contínua que está fortemente relacionada à temperatura (temperatura mais alta = Ozônio mais alto). A função `scale_*_gradient()` é um gradiente sequencial enquanto `scale_*_gradient2()` diverge.

Aqui está o esquema de cores sequencial padrão `{ggplot2}` para variáveis contínuas:

```{r colors-seq}
gb <- ggplot(chic, aes(x = date, y = temp, color = temp)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)", color = "Temperature (°F):")
gb + scale_color_continuous()
```

Este código produz o mesmo gráfico:

```{r colors-seq-alt, eval=FALSE}
gb + scale_color_gradient()
```

E aqui está o esquema de cores padrão divergente:

```{r colors-seq-alt-2}
mid <- mean(chic$temp)  ## midpoint
gb + scale_color_gradient2(midpoint = mid)
```


###### Definir manualmente um esquema de cores sequenciais

Você pode definir manualmente a alteração gradual das paletas de cores para variáveis contínuas via `scale_*_gradient()`:

```{r scale-color-gradient}
gb + scale_color_gradient(low = "darkkhaki",
                          high = "darkgreen")
```

Os dados de temperatura são normalmente distribuídos, então que tal um esquema de cores divergentes (em vez de sequencial) ... Para cores divergentes, você pode usar a função `scale_*_gradient2()`:

```{r scale-color-gradient2}
gb + scale_color_gradient2(midpoint = mid, low = "#dd8a0b",
                           mid = "grey92", high = "#32a676")
```


###### A bela paleta de cores de Viridis

As [**viridis** paletas de cores](https://cran.r-project.org/web/packages/viridis/vignettes/intro-to-viridis.html) não apenas tornam seus gráficos bonitos e bons para percebem, mas também são mais fáceis de ler por aqueles com daltonismo e imprimem bem em escala de cinza.
Você pode testar como seus gráficos podem aparecer sob várias formas de daltonismo usando o pacote [`dichromate`](https://cran.r-project.org/web/packages/dichromat/index.html).

E eles também vêm agora fornecidos com `{ggplot2}`! O gráfico de vários painéis a seguir ilustra três das quatro paletas de viridis:

```{r viridis-continuous, fig.width=12, fig.height=7}
p1 <- gb + scale_color_viridis_c() + ggtitle("'viridis' (default)")
p2 <- gb + scale_color_viridis_c(option = "inferno") + ggtitle("'inferno'")
p3 <- gb + scale_color_viridis_c(option = "plasma") + ggtitle("'plasma'")
p4 <- gb + scale_color_viridis_c(option = "cividis") + ggtitle("'cividis'")
library(patchwork)
(p1 + p2 + p3 + p4) * theme(legend.position = "bottom")
```

Também é possível usar as paletas de cores viridis para variáveis discretas:

```{r viridis-discrete}
ga + scale_color_viridis_d(guide = "none")
```


###### Use paletas de cores quantitativas de pacotes de extensão

Os muitos pacotes de extensão fornecem não apenas paletas de cores categóricas adicionais, mas também paletas sequenciais, divergentes e até cíclicas. Mais uma vez, indico a excelente [coleção fornecida por Emil Hvitfeldt](https://github.com/EmilHvitfeldt/r-color-palettes/blob/master/README.md#comprehensive-list-of-color-palettes- in-r) para uma visão geral.

**Exemplos:**

Os [`{rcartocolors}` pacotes](https://github.com/Nowosad/rcartocolor) portam os lindos [CARTOcolors](https://www.google.com/search?client=firefox-b-d&q=carto + oclors) para `{ggplot2}` e contém várias das minhas paletas mais usadas:

```{r color-carto, fig.width=10}
library(rcartocolor)
g1 <- gb + scale_color_carto_c(palette = "BurgYl")
g2 <- gb + scale_color_carto_c(palette = "Earth")
(g1 + g2) * theme(legend.position = "bottom")
```

O pacote [`{scico}`](https://github.com/thomasp85/scico) fornece acesso às [paletas de cores desenvolvidas por Fabio Crameri](http://www.fabiocrameri.ch/colourmaps.php). Essas paletas de cores não são apenas bonitas e frequentemente incomuns, mas também uma boa escolha, uma vez que foram desenvolvidas para serem perceptivamente uniformes e ordenadas. Além disso, eles funcionam para pessoas com deficiência de visão de cores e em tons de cinza:

```{r color-scico, fig.width=10}
library(scico)
g1 <- gb + scale_color_scico(palette = "berlin")
g2 <- gb + scale_color_scico(palette = "hawaii", direction = -1)
(g1 + g2) * theme(legend.position = "bottom")
```


#### Modificar paletas de cores posteriormente

Desde a última versão do `ggplot2 3.0.0`, pode-se modificar a estética das camadas após terem sido mapeadas para os dados. Ou como o `{ggplot2}` diz: "Use` after_scale() `para sinalizar a avaliação do mapeamento após os dados terem sido escalados."

Então, por que não usar as cores modificadas em primeiro lugar? Uma vez que `{ggplot2}` só pode lidar com uma escala de `color` e uma escala de `fill`, esta é uma funcionalidade interessante. Observe o exemplo a seguir, onde usamos `invert_color()` do pacote [`{ggdark}`](https://github.com/nsgrantham/ggdark):

```{r aftercale}
library(ggdark)
ggplot(chic, aes(date, temp, color = temp)) +
  geom_point(size = 5) +
  geom_point(aes(color = temp,
                 color = after_scale(invert_color(color))),
             size = 2) +
  scale_color_scico(palette = "hawaii", guide = "none") +
  labs(x = "Ano", y = "Temperatura (°F)")
```

Alterar o esquema de cores depois é especialmente divertido com funções dos pacotes `{ggdark}` e `{colorspace}`, nomeadamente `invert_color()`, `lighten()`, `darken()` e `desature()`. Você pode até combinar essas funções. Aqui, traçamos um gráfico de caixa que tem ambos os argumentos, `color` e` fill`:

```{r aftercale-comb}
library(colorspace)
ggplot(chic, aes(date, temp)) +
  geom_boxplot(aes(color = season,
                   fill = after_scale(desaturate(lighten(color, .6), .6))),
               size = 1) +
  scale_color_brewer(palette = "Dark2", guide = "none") +
  labs(x = "Ano", y = "Temperatura (°F)")
```

Note que você precisa especificar a `color` e / ou `fill` em `aes()` do respectivo `geom_*()` ou `stat_*()` para fazer `after_scale()` funcionar.

💡 **Isso parece um pouco complicado por enquanto - pode-se simplesmente usar as escalas `color` e` fill` para ambos. Sim, isso é verdade, mas pense nos casos de uso em que você precisa de várias escalas de `color` e / ou `fill`. Nesse caso, não faria sentido ocupar a escala de `fill` com uma versão ligeiramente mais escura da paleta usada para `color`.**

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Themes {#themes}

##### Alterar o estilo geral de plotagem

Você pode alterar toda a aparência das plotagens usando temas. `{ggplot2}` vem com oito temas integrados:

```{r ggplot2-theme-gallery, echo=FALSE, purl=FALSE, fig.width=11, fig.height=7}
p <- ggplot(chic, aes(x = date, y = temp)) +
       geom_point(color = "gray30", alpha = .3) +
       labs(x = "Ano", y = "Temperatura (°F)")
p1 <- p + theme_gray() + ggtitle("theme_gray()")
p2 <- p + theme_bw() + ggtitle("theme_bw()")
p3 <- p + theme_classic() + ggtitle("theme_classic()")
p4 <- p + theme_dark() + ggtitle("theme_dark()")
p5 <- p + theme_light() + ggtitle("theme_light()")
p6 <- p + theme_linedraw() + ggtitle("theme_linedraw()")
p7 <- p + theme_minimal() + ggtitle("theme_minimal()")
p8 <- p + theme_void() + ggtitle("theme_void()") + theme(plot.title = element_text(color = "black", margin = margin(b = 11/2)))
font_add_google("Roboto Mono", "Roboto Mono")
(p1 + p2 + p3 + p4 + p5 + p6 + p7 + p8) *
  theme(plot.title = element_text(family = "Roboto Mono", hjust = .5, face = "bold", size = 16),
        plot.margin = margin(rep(15, 5))) +
  plot_layout(ncol = 4)
```


Existem vários pacotes que fornecem temas adicionais, alguns até com diferentes paletas de cores padrão. Como exemplo, Jeffrey Arnold reuniu a biblioteca `{ggthemes}` com vários temas personalizados que imitam designs populares. Para obter uma lista, você pode visitar o [`{ggthemes}` site do pacote](https://github.com/jrnold/ggthemes). Sem qualquer codificação, você pode apenas adaptar vários estilos, alguns deles bem conhecidos por seu estilo e estética.

Aqui está um exemplo copiando o [estilo de plotagem](https://www.google.de/search?q=economist+graphic&tbm=isch) na revista [The Economist](http://www.economist.com/) usando `theme_economist()` e `scale_color_economist()`:

```{r Economist}
library(ggthemes)
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  ggtitle("Altos e baixos das temperaturas diárias de Chicago") +
  theme_economist() +
  scale_color_economist(name = NULL)
```

Outro exemplo é o estilo de plotagem de Tufte, um tema minimalista de tinta baseado no livro de [Edward Tufte](http://wwww.aiga.org/medalist-edwardtufte) [The Visual Display of Quantitative Information](https://www.edwardtufte.com/tufte/books_vdqi). Este é o livro que popularizou [o gráfico de Minard representando a marcha de Napoleão na Rússia](https://www.edwardtufte.com/tufte/minard) como um dos **melhores desenhos estatísticos já criados **. Os enredos de Tufte ficaram famosos devido ao purismo de seu estilo. Mas veja você mesmo:

```{r Tufte}
library(dplyr)
chic_2000 <- filter(chic, year == 2000)
ggplot(chic_2000, aes(x = temp, y = o3)) +
  geom_point() +
  labs(x = "Temperatura (°F)", y = "Ozônio") +
  ggtitle("Níveis de temperatura e ozônio durante o ano de 2000 em Chicago") +
  theme_tufte()
```

Reduzi o número de pontos de dados aqui simplesmente para se ajustar ao estilo minimalista de Tufte. Se você gosta da maneira de plotar, dê uma olhada em [esta entrada do blog](http://motioninsocial.com/tufte/) como criar vários plots de Tufte em R.

Outro pacote bacana com temas modernos e uma predefinição de fontes não padrão é o pacote [`{hrbrthemes}` de Bob Rudis](https://github.com/hrbrmstr/hrbrthemes) com vários temas claros, mas também escuros:

```{r hrbrthemes, echo=-5}
library(hrbrthemes)
ggplot(chic, aes(x = temp, y = o3)) +
  geom_point(aes(color = dewpoint), show.legend = FALSE) +
  labs(x = "Temperatura (°F)", y = "Ozônio") +
  ggtitle("Níveis de temperatura e ozônio em Chicago")
update_geom_defaults("point", list(colour = "black"))
```


##### Alterar a fonte de todos os elementos de texto

É incrivelmente fácil alterar as configurações de todos os elementos de texto de uma vez. Todos os temas vêm com um argumento chamado `base_family`:

```{r theme-base-text-family}
g <- ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "firebrick") +
  labs(x = "Ano", y = "Temperatura (°F)",
       title = "Temperaturas em Chicago")
g + theme_bw(base_family = "Playfair")
```


##### Alterar o tamanho de todos os elementos de texto

As funções `theme_*()` também vêm com vários outros argumentos `base_*`. Se você olhar mais de perto o tema padrão (consulte o capítulo "Criar e usar seu tema personalizado" abaixo), você notará que os tamanhos de todos os elementos são relativos `(rel())` ao `base_size`. Como resultado, você pode simplesmente alterar o `base_size` se quiser aumentar a legibilidade de seus gráficos:

```{r theme-base-text-size}
g + theme_bw(base_size = 30, base_family = "Roboto Condensed")
```


##### Alterar o tamanho de todas as linhas e elementos retos

Da mesma forma, você pode alterar o tamanho de todos os elementos do tipo `line` e` rect`:

```{r theme-base-line-size}
g + theme_bw(base_line_size = 1, base_rect_size = 1)
```


##### Crie seu próprio tema

Se você quiser mudar o tema para uma sessão inteira, você pode usar `theme_set` como em` theme_set(theme_bw()) `. O padrão é chamado `theme_gray` (ou`theme_gray`). Se você quiser criar seu próprio tema personalizado, poderá extrair o código diretamente do tema cinza e modificá-lo. Observe que a função `rel()` altera os tamanhos em relação ao `base_size`.
```{r theme-default}
theme_gray
```

Agora, vamos modificar a função do tema padrão e dar uma olhada no resultado:

```{r theme-mods, results="hide"}
theme_custom <- function (base_size = 12, base_family = "Roboto Condensed") {
  half_line <- base_size/2
  theme(
    line = element_line(color = "black", size = .5,
                        linetype = 1, lineend = "butt"),
    rect = element_rect(fill = "white", color = "black",
                        size = .5, linetype = 1),
    text = element_text(family = base_family, face = "plain",
                        color = "black", size = base_size,
                        lineheight = .9, hjust = .5, vjust = .5,
                        angle = 0, margin = margin(), debug = FALSE),
    axis.line = element_blank(),
    axis.line.x = NULL,
    axis.line.y = NULL,
    axis.text = element_text(size = base_size * 1.1, color = "gray30"),
    axis.text.x = element_text(margin = margin(t = .8 * half_line/2),
                               vjust = 1),
    axis.text.x.top = element_text(margin = margin(b = .8 * half_line/2),
                                   vjust = 0),
    axis.text.y = element_text(margin = margin(r = .8 * half_line/2),
                               hjust = 1),
    axis.text.y.right = element_text(margin = margin(l = .8 * half_line/2),
                                     hjust = 0),
    axis.ticks = element_line(color = "gray30", size = .7),
    axis.ticks.length = unit(half_line / 1.5, "pt"),
    axis.ticks.length.x = NULL,
    axis.ticks.length.x.top = NULL,
    axis.ticks.length.x.bottom = NULL,
    axis.ticks.length.y = NULL,
    axis.ticks.length.y.left = NULL,
    axis.ticks.length.y.right = NULL,
    axis.title.x = element_text(margin = margin(t = half_line),
                                vjust = 1, size = base_size * 1.3,
                                face = "bold"),
    axis.title.x.top = element_text(margin = margin(b = half_line),
                                    vjust = 0),
    axis.title.y = element_text(angle = 90, vjust = 1,
                                margin = margin(r = half_line),
                                size = base_size * 1.3, face = "bold"),
    axis.title.y.right = element_text(angle = -90, vjust = 0,
                                      margin = margin(l = half_line)),
    legend.background = element_rect(color = NA),
    legend.spacing = unit(.4, "cm"),
    legend.spacing.x = NULL,
    legend.spacing.y = NULL,
    legend.margin = margin(.2, .2, .2, .2, "cm"),
    legend.key = element_rect(fill = "gray95", color = "white"),
    legend.key.size = unit(1.2, "lines"),
    legend.key.height = NULL,
    legend.key.width = NULL,
    legend.text = element_text(size = rel(.8)),
    legend.text.align = NULL,
    legend.title = element_text(hjust = 0),
    legend.title.align = NULL,
    legend.position = "right",
    legend.direction = NULL,
    legend.justification = "center",
    legend.box = NULL,
    legend.box.margin = margin(0, 0, 0, 0, "cm"),
    legend.box.background = element_blank(),
    legend.box.spacing = unit(.4, "cm"),
    panel.background = element_rect(fill = "white", color = NA),
    panel.border = element_rect(color = "gray30",
                                fill = NA, size = .7),
    panel.grid.major = element_line(color = "gray90", size = 1),
    panel.grid.minor = element_line(color = "gray90", size = .5,
                                    linetype = "dashed"),
    panel.spacing = unit(base_size, "pt"),
    panel.spacing.x = NULL,
    panel.spacing.y = NULL,
    panel.ontop = FALSE,
    strip.background = element_rect(fill = "white", color = "gray30"),
    strip.text = element_text(color = "black", size = base_size),
    strip.text.x = element_text(margin = margin(t = half_line,
                                                b = half_line)),
    strip.text.y = element_text(angle = -90,
                                margin = margin(l = half_line,
                                                r = half_line)),
    strip.text.y.left = element_text(angle = 90),
    strip.placement = "inside",
    strip.placement.x = NULL,
    strip.placement.y = NULL,
    strip.switch.pad.grid = unit(0.1, "cm"),
    strip.switch.pad.wrap = unit(0.1, "cm"),
    plot.background = element_rect(color = NA),
    plot.title = element_text(size = base_size * 1.8, hjust = .5,
                              vjust = 1, face = "bold",
                              margin = margin(b = half_line * 1.2)),
    plot.title.position = "panel",
    plot.subtitle = element_text(size = base_size * 1.3,
                                 hjust = .5, vjust = 1,
                                 margin = margin(b = half_line * .9)),
    plot.caption = element_text(size = rel(0.9), hjust = 1, vjust = 1,
                                margin = margin(t = half_line * .9)),
    plot.caption.position = "panel",
    plot.tag = element_text(size = rel(1.2), hjust = .5, vjust = .5),
    plot.tag.position = "topleft",
    plot.margin = margin(base_size, base_size, base_size, base_size),
    complete = TRUE
  )
}
```

💡 **Você só pode sobrescrever os padrões de todos os elementos que deseja alterar. Aqui listei tudo para que você possa ver que pode mudar *literalmente* mudar tudo! **

Dê uma olhada na estética modificada com sua nova aparência de painel e linhas de grade, bem como sinais de eixos, textos e títulos:

```{r theme-set}
theme_set(theme_custom())
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() + labs(x = "Ano", y = "Temperatura (°F)") + guides(color = FALSE)
```

**Esta forma de alterar o desenho do gráfico é altamente recomendada!**

Ela permite que você altere rapidamente qualquer elemento de seus lotes, alterando-o uma vez. Você pode em poucos segundos plotar todos os seus resultados em um estilo congruente e adaptá-lo a outras necessidades (por exemplo, uma apresentação com tamanho de fonte maior ou requisitos de periódico).


##### Atualizar o tema atual

Você também pode definir mudanças rápidas usando `theme_update()`:

```{r theme-update}
theme_custom <- theme_update(panel.background = element_rect(fill = "gray60"))
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point() + labs(x = "Ano", y = "Temperatura (°F)") + guides(color = FALSE)
```

Para exercícios posteriores, usaremos nosso próprio tema com preenchimento branco e sem as linhas de grade secundárias:

```{r theme-reset}
theme_custom <- theme_update(panel.background = element_rect(fill = "white"),
                             panel.grid.major = element_line(size = .5),
                             panel.grid.minor = element_blank())
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Linhas {#lines}

##### Adicionar linhas horizontais ou verticais a um gráfico

Você pode querer destacar um determinado intervalo ou limite, o que pode ser feito traçando uma linha em coordenadas definidas usando `geom_hline()` (para "linhas horizontais") ou `geom_vline()` (para "linhas verticais"):

```{r hline}
ggplot(chic, aes(x = date, y = temp, color = o3)) +
  geom_point() +
  geom_hline(yintercept = c(0, 73)) +
  labs(x = "Ano", y = "Temperatura (°F)")
```

```{r vline}
g <- ggplot(chic, aes(x = temp, y = dewpoint)) +
  geom_point(color = "dodgerblue", alpha = .5) +
  labs(x = "Temperatura (°F)", y = "Dewpoint")
g +
  geom_vline(aes(xintercept = median(temp)), size = 1.5,
             color = "firebrick", linetype = "dashed") +
  geom_hline(aes(yintercept = median(dewpoint)), size = 1.5,
             color = "firebrick", linetype = "dashed")
```

Se você quiser adicionar uma linha com uma inclinação que não seja 0 ou 1, respectivamente, você precisa usar `geom_abline()`. Este é o caso, por exemplo, se você deseja adicionar uma linha de regressão usando os argumentos `interceptar` e` inclinação`:

```{r abline}
reg <- lm(dewpoint ~ temp, data = chic)
g +
  geom_abline(intercept = coefficients(reg)[1],
              slope = coefficients(reg)[2],
              color = "darkorange2", size = 1.5) +
  labs(title = paste0("y = ", round(coefficients(reg)[2], 2),
                      " * x + ", round(coefficients(reg)[1], 2)))
```

Mais tarde, aprenderemos como adicionar um ajuste linear com um comando usando `stat_smooth(method = "lm")`. No entanto, pode haver outras razões para adicionar uma linha com uma determinada inclinação e é assim que se faz 🤷

##### Adicionar uma linha dentro de um gráfico

As abordagens anteriores sempre cobriram toda a gama do painel de plotagem, mas às vezes queremos destacar apenas uma determinada área ou usar linhas para anotações. Neste caso, `geom_linerange()` está aqui para ajudar:

```{r linerange}
g +
  ## vertical line
  geom_linerange(aes(x = 50, ymin = 20, ymax = 55),
                 color = "steelblue", size = 2) +
  ## horizontal line
  geom_linerange(aes(xmin = -Inf, xmax = 25, y = 0),
                 color = "red", size = 1)
```

Ou você pode usar `geom_segment()` para desenhar linhas com uma inclinação diferente de 0 e 1:

```{r segment}
g +
  geom_segment(aes(x = 50, xend = 75,
                   y = 20, yend = 45),
               color = "purple", size = 2)
```


##### Adicionar linhas curvas e setas a um gráfico

`geom_curve()` adiciona curvas. Bem, e linhas retas se você quiser:

```{r curve}
g +
  geom_curve(aes(x = 0, y = 60, xend = 75, yend = 0),
             size = 2, color = "tan") +
  geom_curve(aes(x = 0, y = 60, xend = 75, yend = 0),
             curvature = -0.7, angle = 45,
             color = "darkgoldenrod1", size = 1) +
  geom_curve(aes(x = 0, y = 60, xend = 75, yend = 0),
             curvature = 0, size = 1.5)
```

O mesmo geom pode ser usado para desenhar setas:

```{r arrows}
g +
  geom_curve(aes(x = 0, y = 60, xend = 75, yend = 0),
             size = 2, color = "tan",
             arrow = arrow(length = unit(0.07, "npc"))) +
  geom_curve(aes(x = 5, y = 55, xend = 70, yend = 5),
             curvature = -0.7, angle = 45,
             color = "darkgoldenrod1", size = 1,
             arrow = arrow(length = unit(0.03, "npc"),
                           type = "closed",
                           ends = "both"))
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Text {#text}

##### Adicionar rótulos aos seus dados

Às vezes, queremos rotular nossos pontos de dados. Para evitar sobreposição e aglomeração por rótulos de texto, usamos uma amostra de 1% dos dados originais, representando igualmente as quatro estações. Estamos usando `geom_label()` que vem com uma nova estética chamada `label`:

```{r data-text}
set.seed(2020)
library(dplyr)
sample <- chic %>%
  dplyr::group_by(season) %>%
  dplyr::sample_frac(0.01)
## code without pipes:
## sample <- sample_frac(group_by(chic, season), .01)
ggplot(sample, aes(x = date, y = temp, color = season)) +
  geom_point() +
  geom_label(aes(label = season), hjust = .5, vjust = -.5) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  xlim(as.Date(c('1997-01-01', '2000-12-31'))) +
  ylim(c(0, 90)) +
  theme(legend.position = "none")
```

Ok, evitar a sobreposição de rótulos não funcionou. Mas não se preocupe, vamos consertar em um minuto!

<details>
  <summary>&#128129; <i>Você também pode usar `geom_text()` se voce não quiser caixas ao redor do seus rótulos. Expanda para ver o exemplo</i></summary>
```{r data-label}
ggplot(sample, aes(x = date, y = temp, color = season)) +
  geom_point() +
  geom_text(aes(label = season), fontface = "bold",
            hjust = .5, vjust = -.25) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  xlim(as.Date(c('1997-01-01', '2000-12-31'))) +
  ylim(c(0, 90)) +
  theme(legend.position = "none")
```
</details>

Uma coisa legal é o pacote `{ggrepel}` que fornece geoms para `{ggplot2}` para repelir texto sobreposto como em nossos exemplos acima. Simplesmente substituímos `geom_text()` por `geom_text_repel()` e `geom_label()` por `geom_label_repel()`:

```{r ggrepel}
library(ggrepel)
ggplot(sample, aes(x = date, y = temp, color = season)) +
  geom_point() +
  geom_label_repel(aes(label = season), fontface = "bold") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.position = "none")
```

Pode parecer melhor com caixas preenchidas, então mapeamos `season` para `fill` em vez de `color` e definimos uma cor branca para o texto:

```{r ggrepel-filled}
ggplot(sample, aes(x = date, y = temp)) +
  geom_point(data = chic, size = .5) +
  geom_point(aes(color = season), size = 1.5) +
  geom_label_repel(aes(label = season, fill = season),
                   color = "white", fontface = "bold",
                   segment.color = "grey30") +
  labs(x = "Ano", y = "Temperatura (°F)") +
  theme(legend.position = "none")
```

Isso também funciona para rótulos de texto puro usando `geom_text_repel()`. Dê uma olhada em todos os [exemplos de uso](https://cran.r-project.org/web/packages/ggrepel/vignettes/ggrepel.html).

##### Adicionar anotações de texto

Existem várias maneiras de adicionar anotações a um ggplot. Podemos novamente usar `geom_text()` ou `geom_label()`:

```{r textbox-label}
g <-
  ggplot(chic, aes(x = temp, y = dewpoint)) +
  geom_point(alpha = .5) +
  labs(x = "Temperatura (°F)", y = "Dewpoint")
g +
  geom_text(aes(x = 25, y = 60,
                label = "Esta é uma anotação útil"))
```

No entanto, agora o ggplot desenhou um rótulo de texto por ponto de dados - são 1.461 rótulos e você só vê um! Você pode resolver isso definindo o argumento `stat` como`"exclusivo"`:

```{r textbox-label-unique}
g +
  geom_text(aes(x = 25, y = 60,
                label = "Esta é uma anotação útil"),
            stat = "unique")
```

A propósito, é claro que pode-se alterar as propriedades do texto exibido:

```{r textbox-label-custom}
g +
  geom_text(aes(x = 25, y = 60,
                label = "Esta é uma anotação útil"),
            stat = "unique", family = "Bangers",
            size = 7, color = "darkcyan")
```

No caso de você usar uma das funções de faceta para visualizar seus dados, você pode ter problemas. Uma coisa é que você pode querer incluir a anotação apenas uma vez:

```{r label-facet-single}
ann <- data.frame(
  o3 = 30,
  temp = 20,
  season = factor("Summer", levels = levels(chic$season)),
  label = "Aqui está o espaço suficiente\npara algumas anotações"
)
g <- ggplot(chic, aes(x = o3, y = temp)) +
     geom_point() +
     labs(x = "Ozônio", y = "Temperatura (°F)")
g +
  geom_text(data = ann, aes(label = label),
            size = 7, fontface = "bold",
            family = "Roboto Condensed") +
  facet_wrap(~season)
```

Outro desafio são facetas em combinação com escalas livres que podem cortar seu texto:

```{r label-facet-scales-problem}
g +
  geom_text(aes(x = 23, y = 97,
                label = "Esta não é uma anotação útil"),
            size = 5, fontface = "bold") +
  scale_y_continuous(limits = c(NA, 100)) +
  facet_wrap(~season, scales = "free_x")
```

Uma solução é calcular o ponto médio do eixo, aqui `x`, de antemão:

```{r label-facet-scales-problem-solved-prep}
library(tidyverse)
(ann <-
  chic %>%
  group_by(season) %>%
  summarize(o3 = min(o3, na.rm = TRUE) +
              (max(o3, na.rm = TRUE) - min(o3, na.rm = TRUE)) / 2))
ann
```

... e use os dados agregados para especificar o posicionamento da anotação:

```{r label-facet-scales-problem-solved-plot}
g +
  geom_text(data = ann,
            aes(x = o3, y = 97,
                label = "Esta é uma anotação útil"),
            size = 5, fontface = "bold") +
  scale_y_continuous(limits = c(NA, 100)) +
  facet_wrap(~season, scales = "free_x")
```

No entanto, existe uma abordagem mais simples (em termos de consertar os cordões) - mas também leva um tempo para saber o código de cor. O pacote `{grid}` em combinação com `{ggplot2}` 's `annotation_custom()` permite que você especifique a localização com base em coordenadas escalonadas onde 0 é baixo e 1 é alto. `grobTree()` cria um objeto gráfico de grade e `textGrob` cria o objeto gráfico de texto. O valor disso é particularmente evidente quando você tem vários gráficos com escalas diferentes.

```{r grobTree}
library(grid)
my_grob <- grobTree(textGrob("Este texto permanece no lugar!",
                             x = .1, y = .9, hjust = 0,
                             gp = gpar(col = "black",
                                       fontsize = 15,
                                       fontface = "bold")))
g +
  annotation_custom(my_grob) +
  facet_wrap(~season, scales = "free_x") +
  scale_y_continuous(limits = c(NA, 100))
```


##### Use Markdown e renderização HTML para anotações

Novamente, estamos usando o Pacote [`{ggtext}`](https://wilkelab.org/ggtext/) desenvolvido Claus Wilke, que foi projetado para melhorar o suporte de renderização de texto para `{ggplot2}`. O pacote `{ggtext}` define dois novos elementos de tema, `element_markdown()` e `element_textbox()`. O pacote também fornece geoms adicionais. `geom_richtext()` é uma substituição para `geom_text()` e `geom_label()` e renderiza o texto como marcação ...

```{r ggtext-geom-richtext-md}
library(ggtext)
lab_md <- "Este gráfico mostra ** temperatura ** em * ° F * versus ** nível de ozônio ** em * ppm *"
g +
  geom_richtext(aes(x = 35, y = 3, label = lab_md),
                stat = "unique")
```

... ou html:

```{r ggtext-geom-richtext-html}
lab_html <- "&#9733; Este gráfico mostra <b style='color:red;'>temperatura</b> em <i>°F</i> versus <b style='color:blue;'>Nível de Ozônio</b>em <i>ppm</i> &#9733;"
g +
  geom_richtext(aes(x = 33, y = 3, label = lab_html),
                stat = "unique")
```

O geom vem com muitos detalhes que podem ser modificados, como ângulo (o que não é possível no `geom_text()` e `geom_label()`) padrão, propriedades da caixa e propriedades do texto.

```{r ggtext-geom-richtext-modify}
g +
  geom_richtext(aes(x = 10, y = 25, label = lab_md),
                stat = "unique", angle = 30,
                color = "white", fill = "steelblue",
                label.color = NA, hjust = 0, vjust = 0,
                family = "Playfair Display")
```

O outro geom do pacote `{ggtext}` é `geom_textbox()`. Este geom permite empacotamento dinâmico de strings, o que é muito útil para anotações mais longas, como caixas de informações e legendas.

```{r ggtetxt-geom-textbox}
lab_long <- "**Este texto é tão longo**<br><i style='font-size:8pt;color:red;'>que eu nao sei o que continuar escrevendo, e por isso estou escrevendo bobagens aleatórias, para mostrar a idéia de modo prolixo.<br>E ainda há espaço para continuar o falatório sem fim, vou ate digitar une dune tê, salame minguê, sorve colorê, bla bla bla bla bla bla bla bla the end</i>"
g +
  geom_textbox(aes(x = 40, y = 10, label = lab_long),
               width = unit(15, "lines"), stat = "unique")
```

Observe que não é possível girar a caixa de texto (sempre na horizontal) nem alterar a justificativa do texto (sempre alinhado à esquerda).

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Coordinadas {#coords}

##### Inverta um gráfico

É incrivelmente fácil virar o enredo de lado. Aqui eu adicionei o `coord_flip ()` que é tudo que você precisa para inverter o gráfico. Isso faz mais sentido ao usar geom's para representar dados categóricos, por exemplo, gráficos de barras ou, como no exemplo a seguir, gráficos de caixa e bigodes:

```{r flip-coords}
ggplot(chic, aes(x = season, y = o3)) +
  geom_boxplot(fill = "indianred") +
  labs(x = "Season", y = "Ozônio") +
  coord_flip()
```

<details>
  <summary>&#128129; <i>Desde `{ggplot2}` versão 3.0.0 também é possível desenhar geometrias horizontalmente via  argumento `orientation = "y"`. Expanda para ver o examplo.</i></summary>
```{r flip-coords-orientation}
ggplot(chic, aes(x = o3, y = season)) +
  geom_boxplot(fill = "indianred", orientation = "y") +
  labs(x = "Ozônio", y = "Season")
```
</details>


##### Corrigir um eixo

Pode-se fixar a proporção do sistema de coordenadas cartesianas e literalmente forçar uma representação física das unidades ao longo dos eixos x e y:

```{r fixed-axis-equal, fig.height=5}
ggplot(chic, aes(x = temp, y = o3)) +
  geom_point() +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio") +
  scale_x_continuous(breaks = seq(0, 80, by = 20)) +
  coord_fixed(ratio = 1)
```

Desta forma, pode-se garantir não apenas um comprimento de passo fixo nos eixos, mas também que o gráfico exportado tenha a aparência esperada. No entanto, seu gráfico salvo provavelmente contém muitos espaços em branco, caso você não use uma proporção de aspecto adequada:


```{r fixed-axis-scaled, fig.height=4.5, fig.width=9}
ggplot(chic, aes(x = temp, y = o3)) +
  geom_point() +
  labs(x = "Temperatura (°F)", y = "Nivel de Ozônio") +
  scale_x_continuous(breaks = seq(0, 80, by = 20)) +
  coord_fixed(ratio = 1/3) +
  theme(plot.background = element_rect(fill = "grey80"))
```


##### Reverter um eixo

Você também pode reverter facilmente um eixo usando `scale_x_reverse()` ou `scale_y_reverse()`, respectivamente:

```{r reversed-axis}
ggplot(chic, aes(x = date, y = temp, color = o3)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_y_reverse()
```

<details>
  <summary>&#128129; <i>Observe que isso só funcionará para dados contínuos. Se você quiser reverter dados discretos, use a função `fct_rev()` do Pacote [{forcats}](https://forcats.tidyverse.org/). Expanda para ver o exemplo.</i></summary>

```{r disc-axes}
## the default
ggplot(chic, aes(x = temp, y = season)) +
  geom_jitter(aes(color = season),
              orientation = "y", show.legend = FALSE) +
  labs(x = "Temperatura (°F)", y = NULL)
```

```{r rev-disc-axes}
library(forcats)
ggplot(chic, aes(x = temp, y = fct_rev(season))) +
  geom_jitter(aes(color = season),
              orientation = "y", show.legend = FALSE) +
  labs(x = "Temperatura (°F)", y = NULL)
```
</details>


##### Transformar um eixo

... ou transforme o mapeamento linear padrão usando `scale_y_log10()` ou `scale_y_sqrt()`. Como exemplo, aqui está um eixo transformado em log<sub>10</sub> (que apresenta NAs, neste caso, tome cuidado):

```{r log-axis}
ggplot(chic, aes(x = date, y = temp, color = o3)) +
  geom_point() +
  labs(x = "Ano", y = "Temperatura (°F)") +
  scale_y_log10(lim = c(0.1, 100))
```


##### Circularizar um enredo

Também é possível circular (polarizar?) O sistema de coordenadas chamando `coord_polar()`.

```{r polar-coords}
library(tidyverse)
chic %>%
  dplyr::group_by(season) %>%
  dplyr::summarize(o3 = median(o3)) %>%
  ggplot(aes(x = season, y = o3)) +
    geom_col(aes(fill = season), color = NA) +
    labs(x = "", y = "Nível Mediano de Ozônio") +
    coord_polar() +
    guides(fill = FALSE)
```

Este sistema de coordenadas permite desenhar gráficos de pizza também:

```{r pie-chart}
chic_sum <-
  chic %>%
  dplyr::mutate(o3_avg = median(o3)) %>%
  dplyr::filter(o3 > o3_avg) %>%
  dplyr::mutate(n_all = n()) %>%
  dplyr::group_by(season) %>%
  dplyr::summarize(rel = n() / unique(n_all))
ggplot(chic_sum, aes(x = "", y = rel)) +
  geom_col(aes(fill = season), width = 1, color = NA) +
  labs(x = "", y = "Proporção de dias que ultrapassam\no nível mediano de ozônio") +
  coord_polar(theta = "y") +
  scale_fill_brewer(palette = "Set1", name = "Season:") +
  theme(axis.ticks = element_blank(),
        panel.grid = element_blank())
```

Eu sugiro sempre olhar também para o resultado do mesmo código em um sistema de coordenadas cartesianas, que é o padrão, para entender a lógica por trás de `coord_polar()` e `theta`:

```{r pie-chart-cartesian}
ggplot(chic_sum, aes(x = "", y = rel)) +
  geom_col(aes(fill = season), width = 1, color = NA) +
  labs(x = "", y = "Proporção de dias que ultrapassam\no nível mediano de ozônio") +
  #coord_polar(theta = "y") +
  scale_fill_brewer(palette = "Set1", name = "Season:") +
  theme(axis.ticks = element_blank(),
        panel.grid = element_blank())
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Tipos de Gráficos {#charts}

##### Alternativas para um Box Plot

Os boxplots são ótimos, mas podem ser incrivelmente enfadonhos. Além disso, mesmo que você esteja acostumado a ver boxplots, lembre-se de que pode haver muitas pessoas olhando para seu plot que nunca viram um box and whisker plot antes.

<details>
<summary>&#128129; <i>Expanda para uma breve recapitulação dos gráficos de caixa e whiskers plots</i></summary>

Um gráfico de caixa e bigode (às vezes chamado simplesmente de gráfico de caixa) é um método semelhante a um histograma de exibição de dados, inventado por J. Tukey. A **linha média ** espessa indica a mediana, também conhecida como quartil <i>Q<sub>2</sub></i>. Os limites da **caixa ** são determinados pelos quartis inferior e superior, <i>Q<sub>1</sub></i> e <i>Q<sub>3</sub></i>. A caixa contém, portanto, 50% dos dados e é chamada * "intervalo interquartil" * (IQR). O comprimento dos **whiskers ** é determinado pelos valores mais extremos que não são considerados outliers (ou seja, valores que estão dentro de 3/2 vezes o intervalo interquartil). <br>
<br>
</details>

Existem alternativas, mas primeiro estamos traçando um gráfico de caixa comum:

```{r boxplot}
g <-
  ggplot(chic, aes(x = season, y = o3,
                   color = season)) +
    labs(x = "Season", y = "Ozônio") +
    scale_color_brewer(palette = "Dark2", guide = "none")
g + geom_boxplot()
```

###### 1. Alternativa: Gráfico de Pontos

Vamos plotar apenas cada ponto de dados dos dados brutos:

```{r point}
g + geom_point()
```

Não apenas enfadonho, mas pouco informativo. Para melhorar o enredo, pode-se adicionar transparência para lidar com o overplotting:

```{r point-alpha}
g + geom_point(alpha = .1)
```

No entanto, definir a transparência é difícil aqui, pois a sobreposição ainda é muito alta ou os valores extremos não são visíveis. Ruim, então vamos tentar outra coisa.

###### 2. Alternativa: Jitter the Points

Experimente adicionar um pouco de instabilidade aos dados. Eu gosto disso para visualização interna, mas tome cuidado ao usar `jittering` porque você está propositalmente adicionando ruído aos seus dados e isso pode resultar em uma interpretação incorreta dos seus dados.

```{r jitter}
g + geom_jitter(width = .3, alpha = .5)
```

###### 3. Alternativa: Grafícos de violino

Plotagens de violino, semelhantes a plotagens de caixa, exceto que você está usando uma densidade de kernel para mostrar onde você tem a maioria dos dados, são uma visualização útil.

```{r violin}
g + geom_violin(fill = "gray80", size = 1, alpha = .5)
```

###### 4. Alternativa: Combinar gráficos de violino com jitter

Podemos, é claro, combinar as densidades estimadas e os pontos de dados brutos:

```{r violin-jitter, fig.height=6}
g + geom_violin(fill = "gray80", size = 1, alpha = .5) +
    geom_jitter(alpha = .25, width = .3) +
    coord_flip()
```

O pacote [`{ggforce}`](https://ggforce.data-imaginist.com/) fornece as chamadas funções sina, onde a largura do jitter é controlada pela distribuição de densidade dos dados - o que torna o jittering um um pouco mais atraente visualmente:

```{r violin-sina}
library(ggforce)
g + geom_violin(fill = "gray80", size = 1, alpha = .5) +
    geom_sina(alpha = .25) +
    coord_flip()
```

###### 5. Alternativa: Combinar Gráficos de violino com Gráficos de caixa

Para permitir uma estimativa fácil de quantis, também podemos adicionar a caixa do gráfico de caixa dentro dos violinos para indicar 25% -quartil, mediana e 75% - quartil:

```{r violin-jitter-box, fig.height=6}
g + geom_violin(aes(fill = season), size = 1, alpha = .5) +
    geom_boxplot(outlier.alpha = 0, coef = 0,
                 color = "gray40", width = .2) +
    scale_fill_brewer(palette = "Dark2", guide = "none") +
    coord_flip()
```


##### Criar uma representação de tapete para um Gráfico

Um tapete representa os dados de uma única variável quantitativa, exibida como marcas ao longo de um eixo. Na maioria dos casos, é usado além de gráficos de dispersão ou mapas de calor para visualizar a distribuição geral de uma ou ambas as variáveis:

```{r rug}
ggplot(chic, aes(x = date, y = temp,
                 color = season)) +
  geom_point(show.legend = FALSE) +
  geom_rug(show.legend = FALSE) +
  labs(x = "Ano", y = "Temperatura (°F)")
```

```{r rug-2}
ggplot(chic, aes(x = date, y = temp, color = season)) +
  geom_point(show.legend = FALSE) +
  geom_rug(sides = "r", alpha = .3, show.legend = FALSE) +
  labs(x = "Ano", y = "Temperatura (°F)")
```


##### Crie uma matriz de correlação

Existem vários pacotes que permitem criar gráficos de matriz de correlação, alguns também usando a infraestrutura `{ggplot2}` e, portanto, retornando ggplots. Vou mostrar como fazer isso sem pacotes de extensão.

O primeiro passo é criar a matriz de correlação. Aqui, usamos o pacote `{corrr}` que funciona bem com pipes, mas também existem muitos outros por aí. Estamos usando Pearson porque todas as variáveis são distribuídas de forma razoavelmente normal (mas você pode considerar Spearman se suas variáveis seguirem um padrão diferente). Observe que, uma vez que uma matriz de correlação tem informações redundantes, estamos definindo metade dela como `NA`.

```{r corr-matrix, echo=-5}
library(tidyverse)
corm <-
  chic %>%
  select(death, temp, dewpoint, pm10, o3) %>%
  corrr::correlate(diagonal = 1) %>%
  corrr::shave(upper = FALSE)
corm
```

Agora colocamos a matriz resultante no formato **long** usando a função `pivot_longer()` do pacote `{tidyr}`:

```{r melt-corr-matrix, echo=-3}
corm <- corm %>%
  pivot_longer(
    cols = -term,
    names_to = "colname",
    values_to = "corr"
  ) %>%
  mutate(rowname = fct_inorder(term),
         colname = fct_inorder(colname))
corm
```

Para o gráfico, usaremos `geom_tile()` para o mapa de calor e `geom_text()` para os rótulos:
 
```{r tile-corr-plot}
ggplot(corm, aes(rowname, fct_rev(colname),
                 fill = corr)) +
  geom_tile() +
  geom_text(aes(label = round(corr, 2))) +
  coord_fixed() +
  labs(x = NULL, y = NULL)
```

Gosto de ter uma paleta de cores divergente, centralizada na correlação zero, com o branco indicando dados ausentes. Também gosto de não ter linhas de grade e preenchimento ao redor do mapa de calor, bem como rótulos bem formatados que são coloridos dependendo do preenchimento(fill) subjacente:

```{r tile-corr-plot-polished}
ggplot(corm, aes(rowname, fct_rev(colname),
                 fill = corr)) +
  geom_tile() +
  geom_text(aes(
    label = format(round(corr, 2), nsmall = 2),
    color = abs(corr) < .75
  )) +
  coord_fixed(expand = FALSE) +
  scale_color_manual(values = c("white", "black"),
                     guide = "none") +
  scale_fill_distiller(
    palette = "PuOr", na.value = "white",
    direction = 1, limits = c(-1, 1)
  ) +
  labs(x = NULL, y = NULL) +
  theme(panel.border = element_rect(color = NA, fill = NA),
        legend.position = c(.85, .8))
```
<br>


##### Criar um gráfico de contorno

Os gráficos de contorno são uma boa maneira de exibir limites de valores. Pode-se usá-los para armazenar dados, mostrando a densidade das observações:

```{r contour-outline}
ggplot(chic, aes(temp, o3)) +
  geom_density_2d() +
  labs(x = "Temperatura (°F)", x = "Nível de Ozônio")
```

```{r contour-filled}
ggplot(chic, aes(temp, o3)) +
  geom_density_2d_filled(show.legend = FALSE) +
  coord_cartesian(expand = FALSE) +
  labs(x = "Temperatura (°F)", x = "Nível de Ozônio")
```

Mas agora, estamos traçando dados tridimensionais. Faremos um gráfico dos limites do ponto de orvalho (ou seja, [a temperatura na qual o vapor de água no ar se condensará para formar o orvalho líquido](https://en.wikipedia.org/wiki/Dew_point)) relacionados à temperatura e aos níveis de ozônio:

```{r contour-z}
## interpolate data
library(akima)
fld <- with(chic, interp(x = temp, y = o3, z = dewpoint))
## prepare data in long format
library(reshape2)
df <- melt(fld$z, na.rm = TRUE)
names(df) <- c("x", "y", "Dewpoint")
g <- ggplot(data = df, aes(x = x, y = y, z = Dewpoint))  +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio",
       color = "Dewpoint")
g + stat_contour(aes(color = ..level.., fill = Dewpoint))
```

Surpresa! Como é definido, o ponto de extração é, na maioria dos casos, igual à temperatura medida.

As linhas estão indicando diferentes níveis de pontos desenhados, mas este não é um gráfico bonito e também difícil de ler devido à falta de bordas. Vamos tentar um gráfico de blocos usando a paleta de cores viridis para codificar o ponto de orvalho de cada combinação de nível de ozônio e temperatura:

```{r contour-tile}
g + geom_tile(aes(fill = Dewpoint)) +
    scale_fill_viridis_c(option = "inferno")
```

Como ficaria se combinarmos uma plotagem de contorno e uma plotagem de ladrilhos para preencher a área sob as linhas de contorno?

```{r contour-and-tile}
g + geom_tile(aes(fill = Dewpoint)) +
    stat_contour(color = "white", size = .7, bins = 5) +
    scale_fill_viridis_c()
```


##### Criar um mapa de calor

Similarmente aos nossos primeiros mapas de contorno, pode-se facilmente mostrar as contagens ou densidades de pontos agrupados em uma grade hexagonal via `geom_hex()`:

```{r heatmap-hex}
ggplot(chic, aes(temp, o3)) +
  geom_hex() +
  scale_fill_distiller(palette = "YlOrRd", direction = 1) +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio")
```

Freqüentemente, linhas brancas aparecem no gráfico resultante. Pode-se corrigir isso mapeando também a cor para `..count..` (o padrão) ou `..density..` ...

```{r heatmap-hex-ouline}
ggplot(chic, aes(temp, o3)) +
  geom_hex(aes(color = ..count..)) +
  scale_fill_distiller(palette = "YlOrRd", direction = 1) +
  scale_color_distiller(palette = "YlOrRd", direction = 1) +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio")
```

... ou definindo a mesma cor do contorno para todas as células hexagonais:

```{r heatmap-hex-ouline-grey}
ggplot(chic, aes(temp, o3)) +
  geom_hex(color = "grey") +
  scale_fill_distiller(palette = "YlOrRd", direction = 1) +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio")
```

Também é possível alterar o binning padrão para dentro ou diminuir o número de células hexagonais:

```{r heatmap-hex-bins}
ggplot(chic, aes(temp, o3, fill = ..density..)) +
  geom_hex(bins = 50, color = "grey") +
  scale_fill_distiller(palette = "YlOrRd", direction = 1) +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio")
```

Se você quiser ter uma grade regular, também pode usar `geom_bin2d()` que resume os dados em células retangulares da grade com base em `bins`:
```{r heatmap-rect-bins}
ggplot(chic, aes(temp, o3, fill = ..density..)) +
  geom_bin2d(bins = 15, color = "grey") +
  scale_fill_distiller(palette = "YlOrRd", direction = 1) +
  labs(x = "Temperatura (°F)", y = "Nível de Ozônio")
```


##### Criar um gráfico de cume

*Parcelas de cume (linha)* são um novo tipo de parcela muito popular no momento.

Embora você possa criar esses gráficos com [comandos básicos `{ggplot2}`](https://github.com/halhen/viz-pub/blob/master/sports-time-of-day/2_gen_chart.R), a liderança de popularidade para um pacote que torna mais fácil criar esses gráficos: [`{ggridges}`](https://cran.r-project.org/web/packages/ggridges/index.html). Vamos usar este pacote aqui.

```{r ridges}
library(ggridges)
ggplot(chic, aes(x = temp, y = factor(year))) +
   geom_density_ridges(fill = "gray90") +
   labs(x = "Temperatura (°F)", y = "Ano")
```

Você pode facilmente especificar a sobreposição e as caudas finais usando os argumentos `rel_min_height` e` escala`, respectivamente. O pacote também vem com seu próprio tema (mas eu prefiro construir o meu próprio, consulte o capítulo ["Criar e usar seu tema personalizado"](# temas)). Além disso, alteramos as cores com base no ano para torná-lo mais atraente.

```{r ridges-spec}
ggplot(chic, aes(x = temp, y = factor(year), fill = year)) +
  geom_density_ridges(alpha = .8, color = "white",
                      scale = 2.5, rel_min_height = .01) +
  labs(x = "Temperatura (°F)", y = "Ano") +
  guides(fill = FALSE) +
  theme_ridges()
```

Você também pode se livrar da sobreposição usando valores abaixo de 1 para o argumento de escala (mas isso de alguma forma contradiz a ideia de gráficos de cume ...). Aqui está um exemplo adicional usando o gradiente de cores viridis e o tema embutido:
```{r ridges-viridis}
ggplot(chic, aes(x = temp, y = season, fill = ..x..)) +
  geom_density_ridges_gradient(scale = .9, gradient_lwd = .5,
                               color = "black") +
  scale_fill_viridis_c(option = "plasma", name = "") +
  labs(x = "Temperatura (°F)", y = "Estação") +
  theme_ridges(font_family = "Roboto Condensed", grid = FALSE)
```

Também podemos comparar vários grupos por ridgeline e colori-los de acordo com seu grupo. Isso segue a ideia de [Marc Belzunces](https://twitter.com/marcbeldata/status/888697140268204032).

```{r ridges-groups}
library(tidyverse)
## only plot extreme season using dplyr from the tidyverse
ggplot(data = filter(chic, season %in% c("Summer", "Winter")),
         aes(x = temp, y = year, fill = paste(year, season))) +
  geom_density_ridges(alpha = .7, rel_min_height = .01,
                      color = "white", from = -5, to = 95) +
  scale_fill_cyclical(breaks = c("1997 Summer", "1997 Winter"),
                      labels = c(`1997 Summer` = "Summer",
                                 `1997 Winter` = "Winter"),
                      values = c("tomato", "dodgerblue"),
                      name = "Season:", guide = "legend") +
  theme_ridges(grid = FALSE) +
  labs(x = "Temperatura (°F)", y = "Ano")
```

O pacote `{ggridges}` também é útil para criar histogramas para grupos diferentes usando `stat =" binline"` no comando `geom_density_ridges()`:

```{r ridges-histo}
ggplot(chic, aes(x = temp, y = factor(year), fill = year)) +
  geom_density_ridges(stat = "binline", bins = 25, scale = .9,
                      draw_baseline = FALSE, show.legend = FALSE) +
  theme_minimal() +
  labs(x = "Temperatura (°F)", y = "Season")
```

[↑ Voltar para o Índice.](#toc)<br><br>



## Trabalhando com Ribbons (AUC, CI, etc.) {#ribbons}

Este não é um conjunto de dados perfeito para demonstrar isso, mas usar a faixa de opções pode ser útil. Neste exemplo, criaremos uma média de execução de 30 dias usando a função `filter()` para que nossa fita não seja muito barulhenta.

```{r ribbon-default}
chic$o3run <- as.numeric(stats::filter(chic$o3, rep(1/30, 30), sides = 2))
ggplot(chic, aes(x = date, y = o3run)) +
   geom_line(color = "chocolate", lwd = .8) +
   labs(x = "Ano", y = "Ozônio")
```

Como ficaria se preenchermos a área abaixo da curva usando a função `geom_ribbon()`?

```{r geom-ribbon-AUC}
ggplot(chic, aes(x = date, y = o3run)) +
   geom_ribbon(aes(ymin = 0, ymax = o3run),
               fill = "orange", alpha = .4) +
   geom_line(color = "chocolate", lwd = .8) +
   labs(x = "Ano", y = "Ozônio")
```

É bom indicar a [área sob a curva (AUC)](https://en.wikipedia.org/wiki/Area_under_the_curve_ (farmacocinética)), mas esta não é a maneira convencional de usar `geom_ribbon()`.

💁 *E, na verdade, uma maneira melhor de conseguir o mesmo é `geom_are()`. *

<details>
  <summary>Expanda para ver o exemplo.</summary>
```{r geom-area-AUC}
ggplot(chic, aes(x = date, y = o3run)) +
   geom_area(color = "chocolate", lwd = .8,
             fill = "orange", alpha = .4) +
   labs(x = "Ano", y = "Ozônio")
```
</details>

Em vez disso, desenhamos uma fita que nos dá um desvio padrão acima e abaixo de nossos dados:

```{r geom-ribbon-range}
chic$mino3 <- chic$o3run - sd(chic$o3run, na.rm = TRUE)
chic$maxo3 <- chic$o3run + sd(chic$o3run, na.rm = TRUE)
ggplot(chic, aes(x = date, y = o3run)) +
   geom_ribbon(aes(ymin = mino3, ymax = maxo3), alpha = .5,
               fill = "darkseagreen3", color = "transparent") +
   geom_line(color = "aquamarine4", lwd = .7) +
   labs(x = "Ano", y = "Ozônio")
```

[↑ Voltar para o Índice.](#toc)<br><br> 



## Trabalhando com Smoothings {#smooths}

É incrivelmente fácil adicionar Smoothings/suavização aos seus dados usando `{ggplot2}`.


##### Padrão: Adicionando um LOESS ou GAM Smoothing

Você pode simplesmente usar `stat_smooth()` - nem mesmo uma fórmula é necessária. Isso adiciona um LOESS (suavização do gráfico de dispersão com peso local, `method = "loess"`) se você tiver menos de 1000 pontos ou um GAM (modelo aditivo generalizado, `method = "gam"`) caso contrário. Como temos mais de 1000 pontos, a suavização é baseada em um GAM:

```{r stat-smooth, message=TRUE}
ggplot(chic, aes(x = date, y = temp)) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  stat_smooth() +
  geom_point(color = "gray40", alpha = .5)
```
💡 ** Na maioria dos casos, deseja-se que os pontos fiquem no topo da fita, então certifique-se de sempre chamar a suavização antes de adicionar os pontos. **

##### Adicionando um ajuste linear

Embora o padrão seja uma suavização LOESS ou GAM, também é fácil adicionar um ajuste linear padrão:

```{r LM}
ggplot(chic, aes(x = temp, y = death)) +
   labs(x = "Temperatura (°F)", y = "Deaths") +
   stat_smooth(method = "lm", se = FALSE,
               color = "firebrick", size = 1.3) +
   geom_point(color = "gray40", alpha = .5)
```


##### Especificando a fórmula para suavização

`{ggplot2}` permite que você especifique o modelo que deseja usar. Talvez você queira usar uma [regressão polinomial](https://en.wikipedia.org/wiki/Polynomial_regression)?
```{r GAM-spec1}
ggplot(chic, aes(x = o3, y = temp))+
  labs(x = "Nível de Ozônio", y = "Temperatura (°F)") +
  geom_smooth(
    method = "lm",
    formula = y ~ x + I(x^2) + I(x^3) + I(x^4) + I(x^5),
    color = "black",
    fill = "firebrick"
  ) +
  geom_point(color = "gray40", alpha = .3)
```

<details>
  <summary>&#128129; <i>Huh, `geom_smooth()`? Há uma diferença importante entre as camadas `geom` e` stat`, mas aqui realmente não importa qual você usa. Expanda para comparar os dois.</i></summary>
```{r geom-stat-smooth-a}
ggplot(chic, aes(x = o3, y = temp))+
  labs(x = "Nível de Ozônio", y = "Temperatura (°F)") +
  geom_smooth(stat = "smooth") + ## the default
  geom_point(color = "gray40", alpha = .3)
```

```{r geom-stat-smooth-b}
ggplot(chic, aes(x = o3, y = temp))+
  labs(x = "Nível de Ozônio", y = "Temperatura (°F)") +
  stat_smooth(geom = "smooth") + ## the default
  geom_point(color = "gray40", alpha = .3)
```
</details>

Ou digamos que você queira aumentar a dimensão GAM (adicione alguns movimentos adicionais para suavizar):

```{r GAM-spec2, include=TRUE, cache=TRUE}
cols <- c("darkorange2", "firebrick", "dodgerblue3")
ggplot(chic, aes(x = date, y = temp)) +
  geom_point(color = "gray40", alpha = .3) +
  labs(x = "Ano", y = "Temperatura (°F)") +
  stat_smooth(aes(col = "1000"),
              method = "gam",
              formula = y ~ s(x, k = 1000),
              se = FALSE, size = 1.3) +
  stat_smooth(aes(col = "100"),
              method = "gam",
              formula = y ~ s(x, k = 100),
              se = FALSE, size = 1) +
  stat_smooth(aes(col = "10"),
              method = "gam",
              formula = y ~ s(x, k = 10),
              se = FALSE, size = .8) +
  scale_color_manual(name = "k", values = cols)
```

[↑ Voltar para o Índice.](#toc)<br><br>


## Trabalhando com Parcelas Interativas {#interactive}

A coleção a seguir lista bibliotecas que podem ser usadas em combinação com `{ggplot2}` ou por conta própria para criar visualizações interativas em R (geralmente usando bibliotecas JavaScript existentes).


##### Combinação de {ggplot2} e {shiny}

`{shiny}` é um pacote do [RStudio](https://rstudio.com/) que torna incrivelmente fácil construir aplicativos da web interativos com R. Para uma introdução e exemplos ao vivo, visite a [página inicial do Shiny](http : //shiny.rstudio.com/).

Para ver o uso potencial, você pode verificar os exemplos do Hello Shiny. Este é o primeiro:

```{r shiny, eval=FALSE}
library(shiny)
runExample("01_hello")
```

Claro, pode-se usar ggplots nesses aplicativos. Este exemplo demonstra a possibilidade de adicionar alguma experiência de usuário interativa:

```{r shiny-04, eval=FALSE}
runExample("04_mpg")
```


##### Plot.ly via {plotly} e {ggplot2}

[Plot.ly](https://chart-studio.plotly.com/feed/#/) é uma ferramenta para criar aplicativos gráficos e web interativos on-line. O pacote [`{plotly}`](https://plot.ly/r/getting-started/) permite que você os crie diretamente de seus gráficos `{ggplot2}` e o fluxo de trabalho é surpreendentemente fácil e [pode ser feito de dentro de R](https://plotly-r.com/). No entanto, algumas das configurações do seu tema podem ser alteradas e precisam ser modificadas manualmente posteriormente. Além disso, e infelizmente, não é fácil criar facetas ou verdadeiros gráficos de múltiplos painéis que escalam bem.

```{r plotly, echo=-3}
g <- ggplot(chic, aes(date, temp)) +
  geom_line(color = "grey") +
  geom_point(aes(color = season)) +
  scale_color_brewer(palette = "Dark2", guide = "none") +
  labs(x = NULL, y = "Temperatura (°F)") +
  theme_bw()
g
library(plotly)
ggplotly(g)
```

Aqui, por exemplo, ele mantém a configuração geral do tema, mas adiciona a legenda novamente.


##### ggiraph e ggplot2

[`{ggiraph}`](https://davidgohel.github.io/ggiraph/index.html) é um pacote R que permite criar gráficos `{ggplot2}` dinâmicos. Isso permite que você adicione dicas de ferramentas, animações e ações JavaScript aos gráficos. O pacote também permite a seleção de elementos gráficos quando usados em aplicativos Shiny.

```{r ggiraph}
library(ggiraph)
g <- ggplot(chic, aes(date, temp)) +
  geom_line(color = "grey") +
  geom_point_interactive(
    aes(color = season, tooltip = season, data_id = season)
  ) +
  scale_color_brewer(palette = "Dark2", guide = "none") +
  labs(x = NULL, y = "Temperatura (°F)") +
  theme_bw()
girafe(ggobj = g)
```

##### Highcharts via {highcharter}

[Highcharts](https://www.highcharts.com/), uma biblioteca de software para gráficos interativos, é outra biblioteca de visualização escrita em JavaScript puro que foi portado para R. O pacote [`{highcharter}`](https: //jkunst.com/highcharter/) torna possível usá-los - mas esteja ciente de que Highcharts só é gratuito em caso de uso não comercial.

```{r highcharter}
library(highcharter)
hchart(chic, "scatter", hcaes(x = date, y = temp, group = season))
```


##### Echarts via {echarts4r}

[Apache ECharts](https://echarts.apache.org/en/index.html) é uma biblioteca gratuita e poderosa de gráficos e visualização que oferece uma maneira fácil de construir gráficos intuitivos, interativos e altamente personalizáveis. Mesmo que seja escrito em JavaScript puro, pode-se usá-lo em R através da biblioteca [`{echarts4r}`](https://echarts4r.john-coene.com/) graças a [John Coene](https://john-coene.com/). Confira a impressionante [galeria de exemplo](https://echarts4r.john-coene.com/articles/chart_types.html) ou esses dois aplicativos ([App 1](https://johncoene.shinyapps.io/fopi-contest/) e [App 2](https://berlinbikes.correlaid.org/)) fazendo uso da funcionalidade `{echarts4r}`.

```{r echarts4r}
library(echarts4r)
chic %>%
  e_charts(date) %>%
  e_scatter(temp, symbol_size = 7) %>%
  e_visual_map(temp) %>%
  e_y_axis(name = "Temperatura (°F)") %>%
  e_legend(FALSE)
```


##### Chart.js via {charter}

[charter](https://github.com/JohnCoene/charter) é outro pacote desenvolvido por John Coene que permite o uso de uma biblioteca de visualização JavaScript em R. O pacote permite que você construa gráficos interativos com a ajuda dos [Charts.js framework](https://www.chartjs.org/).

```{r charter, eval=FALSE}
library(charter)
chic$date_num <- as.numeric(chic$date)
## doesn't work with class date
chart(data = chic, caes(date_num, temp)) %>%
  c_scatter(caes(color = season, group = season)) %>%
  c_colors(RColorBrewer::brewer.pal(4, name = "Dark2"))
```
(The example doesn't work in Rmarkdown.)


[↑ Voltar para o Índice.](#toc)<br><br>


## Remarks, Tipps & Resources {#tipps}

##### Using `ggplot()` in Loops and Functions

The grid-based graphics functions in lattice and ggplot2 create a graph object. When you use these functions interactively at the command line, the result is automatically printed, but in `source()` or inside your own functions you will need an explicit `print()` statement, i.e. `print(g)` in most of our examples. See also the [Q&A page of R](https://cran.r-project.org/doc/FAQ/R-FAQ.html#Why-do-lattice_002ftrellis-graphics-not-work_003f).


##### Additional Resources

* ["ggplot2: Elegant Graphics for Data Analysis"](https://ggplot2-book.org/) por Hadley Wickham, disponível em acesso aberto!
* ["Fundamentals of Data Visualization"](http://serialmentor.com/dataviz/) por Claus O. Wilke sobre a visualização de dados em geral, mas usando`{ggplot2}`. (Você pode encontrar os códigos no [GitHub dele](https://github.com/clauswilke/dataviz).)
* ["Cookbook for R"](http://www.cookbook-r.com/Graphs/)
por Winston Chang com receitas para produzir plotagens R
* Galeria do [Top 50 ggplot2 visualizations](http://r-statistics.co/Top50-Ggplot2-Visualizations-MasterList-R-Code.html)
* Galeria do [`{ggplot2}` extension packages](https://exts.ggplot2.tidyverse.org/gallery/)
* [How to extend `{ggplot2}`](https://cran.r-project.org/web/packages/ggplot2/vignettes/extending-ggplot2.html) by Hadley Wickham
* O Fantástico [R4DS Online Learning Community](https://www.rfordatasci.com/) que oferece ajuda e orientação para todas as coisas relacionadas ao conteúdo do ["R for Data Science" book](r4ds.had.co.nz/) de Hadley Wickham
* [#TidyTuesday](https://github.com/rfordatascience/tidytuesday), um projeto semanal de dados sociais com foco em ggplots - verifique também [#TidyTuesday on Twitter](https://twitter.com/hashtag/TidyTuesday?lang=en) e [esta coleção de contribuições de Neil Grantham](https://nsgrantham.com/tidytuesdayrocks/)
* Uma série de tutoriais de duas partes e 4,5 horas por Thomas Linn Pedersen ([Part 1](https://www.youtube.com/watch?v=h29g21z0a68) | [Part 2](https://www.youtube.com/watch?v=0m4yywqNPVY))

[↑ Voltar para o Índice.](#toc)<br><br>


***

<details>
<summary style='font-size:10pt;'>R Session Info</summary>

```{r session-info, echo=FALSE, purl=FALSE}
sessionInfo()
```